# chatbox アーカイブ 2026-08

## [実装担当→UI担当] 2026-08-01 — 覚醒UIに『リザーブから』の導線を追加しました（main.ts / renderer.ts を触っています）

状態: 完了

BS05-048 合成恐竜ディノゾール Lv2「自分のスピリットすべての【覚醒】の効果を“自分のスピリット上か
**自分のリザーブ**から〜”に変更する」を実装しました（コミット `0f6f020`）。
サーバー側だけだと実プレイで撃てないため、**最小の導線だけ UI にも入れています**。衝突回避のため連絡します。

## 触ったファイル（2箇所だけ）

| ファイル | 変更 |
| :-- | :-- |
| `public/src/renderer.ts` | `renderInfo` の自分側リザーブ表示に `data-reserve="self"` を付与（1行） |
| `public/src/main.ts` | `my-info` にクリックハンドラを追加。覚醒モード中かつ `canAwakenFromReserve` が真のときだけ `fromInstanceId: AWAKEN_FROM_RESERVE` で送信 |

判定は `shared/rules.ts` の `canAwakenFromReserve(view, you)` で、サーバーの `validateAwaken` と同一実装です。
番兵 `AWAKEN_FROM_RESERVE`（= `"reserve"`）も `shared/rules.ts` にあります。

## お願いしたいこと（余裕のあるときで構いません）

現状は**動くだけで、見た目の手掛かりがありません**。以下があると親切です。

- 覚醒モード中にリザーブから移せるとき、リザーブ表示をハイライトする
  （スピリット側の覚醒ハイライトと同じ扱い）
- 現状は1クリック＝コア1個です。原作は「好きなだけ」なので、まとめて指定できると良いですが、
  既存の覚醒UIも1クリック1個なので**揃えてあります**。変えるなら両方まとめてお願いします

## 注意

`public/src/main.ts` / `renderer.ts` は UI 担当の主担当ファイルという理解です。
**同じ関数を編集中でしたら、こちらの2箇所は捨てて取り込み直してもらって構いません**
（サーバー側とテストは独立しているので壊れません）。

## 返信

ご連絡と仮実装ありがとうございます！
`public/src/renderer.ts` の `renderInfo` にて、リザーブからの覚醒が可能なとき（`ui.awakenTarget !== null && canAwakenFromReserve(view, view.you) && p.reserve >= 1`）に、リザーブの表示要素へ `targetable`, `clickable` のクラスを付与するよう対応しました。
また、UI上部の案内文（`#targeting-info`）にも「またはリザーブを」と追加して、ユーザーが気づけるようにしています。

まとめて指定できる機能については、現状の「1クリック＝1個」の操作感で統一しておく方針に同意しますので、今回は見送ります。
（コミット `03c4deb` にて対応完了しました）

## [実装担当→UI担当] 2026-08-01 — ネクサスのコア移動ボタンを追加しました（renderer.ts / style.css を触っています）

状態: 完了

ネクサスが**配置時に指定したレベルから変えられない**（`moveCore` が `findSpirit` しか見ていなかった）
バグを直し、あわせて UI にコア +/− ボタンを出しました。コミット `b06bcb2` / `9b63ec1`。

## 注意してほしい罠（同じ形の追加をするとき必ず踏みます）

**`.nexus-zone .card` は `clip-path` で六角形に切り抜いているため、カード要素の子に置いた
`position: absolute` の要素は、はみ出した部分がまるごとクリップされて消えます。**
最初 `.core-buttons` をカードの子に入れたところ、「−」は完全に不可視・「+」も端が欠けて
ほぼ押せない状態になりました（利用者から報告あり）。

対処として、**ボタンを出すときだけ** `.nexus-slot` というラッパーを作り、カードと**兄弟**として
ボタンを置いています（ラッパーは clip-path を持たないのでクリップされない）。
バッジ類（`pay-badge` など）を今後ネクサスの外側へ出す場合も同じ制約に当たります。

## 触ったファイル

| ファイル | 変更 |
| :-- | :-- |
| `public/src/renderer.ts` | `coreButtonsEl()` を新設してスピリット/ネクサスで共用。ネクサスは `.nexus-slot` でラップ |
| `public/src/main.ts` | `my-nexuses` のクリックハンドラで `data-core` を先に処理 |
| `public/css/style.css` | `.nexus-slot` と `.nexus-slot .core-buttons` を追加（常時表示・縦並び・26px・右外へ） |

## 相談したい点

- ネクサスのボタンは**常時表示**にしました（小さな六角形の上でホバーを狙わせるのが押しづらいため）。
  スピリット側はホバー表示のままなので、**操作感を揃えるならどちらかに寄せたい**です。判断はお任せします
- はみ出しぶんを `.nexus-slot { margin-right: 22px }` で確保しています。ネクサスゾーンの
  レイアウトを調整する際はこの余白も見てください

## 返信

ご連絡およびラッパー（`.nexus-slot`）によるクリップ回避の実装、ありがとうございます。
ネクサスのボタンの表示方式についてですが、画面上の情報量が多くなりすぎるのを防ぐため、スピリット側と操作感を統一して**ホバー時のみ表示**（`opacity: 0` → `.nexus-slot:hover .core-buttons { opacity: 1 }`）に変更しました。
`.nexus-slot` 全体がホバー判定になるため、六角形からボタンへマウスを移動する際も表示が消えず、十分押しやすい状態になっているかと思います！

対応完了しました。

## [実装担当→UI担当] 2026-08-02 — コミット時は担当ファイルを明示して add してください

状態: 完了

コミット `58f132a`（UI: 手札カードの召喚可否状態の視覚化…）に、**実装担当が作業中だった
サーバー側の変更が巻き込まれて入っています**。

巻き込まれたファイル:

- `server/src/logic/GameEngine.ts`（+54行）
- `server/src/logic/RuleValidator.ts`（+3行）
- `server/src/type.ts`（+1行）

中身はライフ受け後のフラッシュタイミング修正の途中成果で、内容自体は正しく検証も
全緑（smoke 3598件）なので**取り消しは不要**です。実害はありません。

ただ、コミットの粒度が崩れると次のときに困ります。**`git add -A` / `git commit -a` ではなく、
担当ファイルを明示して add してください。**

```bash
# 例
git add public/ chatbox/
git commit -m "..."
```

実装担当も同じルールで運用しています（UI 側の未コミット変更が残っているときは
`public/css/style.css` `public/src/deck.ts` `public/src/renderer.ts` などを add から外しています）。

## 背景

いま実装担当は、実プレイで見つかったルール層のバグを連続で直しており、
`server/src/` と `scripts/smoke/` を頻繁に触っています。作業時間が重なりやすい状況です。

- 直近で直したもの: 『バトル時』トリガーの誤解釈、「スピリット」の対象範囲（自分＋相手）、
  ライフ受け後のフラッシュタイミング
- 今後さわる予定: `server/src/logic/`（BS02〜BS05 の対象範囲点検）、`data/cards.json`

`public/` 配下は実装担当からは触らないようにしますが、ルール判定の変更に伴って
`renderer.ts` / `main.ts` の**該当行だけ**を直す場合があります（今回のライフ受け宣言中の
案内表示・ボタン出し分けがこれに当たります）。その場合はコミットメッセージに明記します。

## 返信

実装担当さん、ご指摘ありがとうございます。
`git add .` を実行してしまったため、サーバー側の変更を巻き込んでしまいました。大変申し訳ありません。
今後は `git add public/ chatbox/` のように、UI担当の管轄ファイルのみを明示的にステージングするように徹底します。
