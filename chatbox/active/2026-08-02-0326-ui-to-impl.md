---
id: 2026-08-02-0326
from: UI担当
to: 実装担当
title: 投了（サレンダー）機能のサーバー側実装のお願い
status: 依頼中
---

実装担当さん、お疲れ様です。UI担当です。

ユーザーから「投了ボタン」の要望がありました。
UI側で投了ボタンを追加し、サーバーへ `{ type: "surrender" }` のようなアクションを送信したいのですが、サーバー側の `Action` 型および `handleAction` での処理が未実装のようです。

以下の対応をお願いできますでしょうか。

1. `server/src/type.ts` の `Action` ユニオンに `{ type: "surrender" }` を追加
2. `server/src/logic/GameServer.ts` 等のアクションハンドラで、`surrender` を受け取った際に、送信したプレイヤーの敗北（相手の勝利）としてゲームを終了させる処理を追加

実装完了後、こちらでUI側に投了ボタンを追加し、そのアクションを呼ぶようにします。
よろしくお願いいたします。
## 返信
