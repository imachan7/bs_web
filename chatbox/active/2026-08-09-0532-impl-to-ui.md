---
id: 2026-08-09-0532
from: UI担当
to: 実装担当
title: ブラックカラカロッサムの入れ替え召喚UIの追加依頼
status: 作業中
---

第七弾（BS07）を全88枚取り込みました。そのうち **BS07-022 ブラックカラカロッサム** だけは
サーバー側の実装は入っていますが、**クライアントから発動する手段がありません**。UIの追加をお願いします。

## カードの挙動

> 自分の手札のこのスピリットカードは、バトルしている自分の[カラカロッサム]1体を手札に戻すことで、
> 疲労状態で召喚して代わりにバトルする。

フラッシュ中のバトルで、自分の[カラカロッサム]（**カード名の完全一致**）がバトルに参加していれば、
そのスピリットを手札に戻して、このカードを疲労状態で召喚しバトルを引き継ぎます。召喚コストは不要で、
維持コア（Lv1ぶん）だけリザーブから出ます。

## サーバー側のインタフェース

既存の `summon` アクションに**任意フィールドを1つ足しただけ**です。既存の呼び出しは変更不要です。

```ts
{ type: "summon", handIndex: number, substituteInstanceId: string }
```

- `substituteInstanceId` = 手札に戻す自分のスピリット（バトル参加中の[カラカロッサム]）の instanceId
- 指定時は通常の召喚検証を通らず、専用の検証に切り替わります（`paySources` / `level` は無視）
- 発動できない場合はエラー文字列が返ります（例：「入れ替え元のスピリットはバトルに参加していません」
  「入れ替え元は[カラカロッサム]である必要があります」「現在フラッシュの優先権がありません」）

## お願いしたいこと

発動可否の判定を共有層に出していないため、いまはクライアント側で条件を組む必要があります。
判定用のヘルパー（`shared/` に `canBattleSwapSummon(view, you, handIndex)` のようなもの）が
あったほうが作りやすければ、こちらで用意します。**必要かどうかだけ返信してください。**

## 返信

**[UI担当]**
はい、`canBattleSwapSummon(view, you, handIndex)` のような判定ヘルパーを `shared/rules.ts` 等にご用意いただけると大変助かります！
（UI側で対象の判定やフラッシュタイミングの判定を組むと表示バグの原因になりやすいため）

ヘルパーが追加されましたら、UI側で手札クリック時のアクションボタンとして組み込みますので、追加が完了しましたらまたこのメッセージでお知らせください。

**[実装担当]** ヘルパーを追加しました（コミット `46a44d9`）。`shared/summon.ts` です。

```ts
import { canBattleSwapSummon } from "../../shared/summon"

const option = canBattleSwapSummon(view, view.you, handIndex)
// null なら発動不可（ボタンを出さない）
```

返り値:

| フィールド | 中身 |
| :-- | :-- |
| `substituteInstanceIds` | 入れ替え元に選べる自分のスピリットの instanceId。**ここから選ばせてください** |
| `substituteName` | 入れ替え元のカード名（`カラカロッサム`）。文言に使えます |
| `cost` / `placeCores` / `totalCores` | 軽減後の召喚コスト／置く維持コア／その合計 |
| `effectId` | 効果ID（表示用） |

フラッシュ優先権・バトル参加・カード名一致・フラッシュロックはすべて中で見ています。
**サーバーの `validateSummon` も同じ関数を通す**ようにしたので、判定がズレることはありません。

2点だけ注意をお願いします。

1. **1件、仕様が変わりました。召喚コストは通常どおり必要です。**
   前回「召喚コストは不要」とお伝えしましたが誤りでした（効果文に「コストを支払わずに」が無いため、
   [カラカロッサム]を手札に戻すのは**追加コスト**）。送るアクションに `paySources` が要ります。

   ```ts
   { type: "summon", handIndex, substituteInstanceId, paySources }
   ```

   必要なコア数は `totalCores` です。通常召喚と同じ支払い元選択UIをそのまま使えます。
2. **`canBattleSwapSummon` はコアの過不足を見ません**（`totalCores` を返すだけ）。
   足りないときにボタンを消すか、押させてサーバーのエラーを出すかはUI側の判断でお願いします。

ついでに `shared/board.ts` の `BoardPlayer` に `hand: string[] | null` を足しています
（相手ぶんは `GameView` では `null`）。共有層で手札を読むのは自分ぶんだけの想定です。
