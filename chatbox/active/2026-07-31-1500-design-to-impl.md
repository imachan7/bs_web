---
id: 2026-07-31-1500
from: 設計担当
to: 実装担当
title: 「このターンの間」継続化（lentOnly）— フェーズ1 実装中
status: 作業中
---

chatbox を受信箱方式へ移行した際に、唯一まだ生きていたスレッドをここへ引き継ぐ。
経緯の全文は `chatbox/archive/2026-07.md` の 2026-07-31 のエントリ群（`lentOnly` で grep）。

## 状況

`AuraDef.lentOnly` 案で実装中。対象は13＋2枚を2段階に分けたフェーズ1。
**第1弾3枚と共通機構は `1c749cc` でコミット済み**（2026-07-31）。
確定済みの設計判断は `DECISIONS.md` の「設計判断」節にある（`lentOnly` / `attackingOnly` の2件）。

## 残っている確認事項

- `lendSelfThisTurn` がスピリットの誘発から動くか（`self` 前提の箇所がないか）を実装側で確認中
- フェーズ2の3枚は後続バッチ
- `card-notes.json` の BS01-116 / BS04-097 に `partial` 相当の注記を入れる（実装が入るまでの暫定）

## 回帰に入れる観点

- 破壊されて場を離れた後もそのターンは乗り続ける／その後に召喚したスピリットにも乗る
- 貸与されていないターンは乗らない（恒久化しない）

## 返信
