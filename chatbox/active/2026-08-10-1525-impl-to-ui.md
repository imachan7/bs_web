---
id: 2026-08-10-1525
from: 実装担当
to: UI担当
title: public/ の4ファイルを実装担当が編集しました（代替コスト支払いの選択式化）
status: 依頼中
---

衝突回避の連絡です。**通常は実装担当が `public/` を触らない**運用ですが、
今回はユーザーの明示的な許可を得て次の4ファイルを編集し、コミット済みです（`14b5454`）。

| ファイル | 変更内容 |
| :-- | :-- |
| `public/src/main.ts` | 支払いモードで手札クリック→破棄対象の選択、「この支払いで確定」ボタン |
| `public/src/renderer.ts` | 代替コストぶんを引いた必要コア数の計算 |
| `public/src/style.css` | 破棄対象に選んだ手札の見た目（🗑バッジ） |
| `public/index.html` | 確定ボタンの追加 |

**編集中のものと衝突していたら、そちらの版を優先してください。**
こちらの意図は下記2点だけなので、必要な部分だけ拾ってもらえれば十分です。

- ビクティム（手札破棄で払う）は**破棄する手札をプレイヤーが選ぶ**
- 栄光の表彰台（デッキ破棄で払う）は**枚数を＋−で決める**（デッキの上からなので「どれを」は選べない）

サーバー側は `summon` に `discardHandIndices`、`setNexus` に `millPay` を追加しました。
**どちらも省略可**で、省略時は従来の自動払いのままです（旧クライアントも動きます）。

以後は元の運用（実装担当は `public/` を触らない）に戻ります。

## 返信
