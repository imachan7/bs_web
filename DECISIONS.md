# 決定事項の台帳

エージェント間のやりとり（`chatbox/`）で確定した判断のうち、**あとから読む必要があるものだけ**を
1件1〜3行で残す。chatbox 本体は流れて消える前提なので、結論はここに落とす。

**SPEC.md と重複させないこと。** 仕様として SPEC.md に書かれたものはここから消し、
必要ならポインタだけ残す。ここは「SPEC に昇格する前」または「SPEC に書く粒度ではない」判断の置き場。

書き方: `- <結論>（<日付> 確定／出典: chatbox/archive/YYYY-MM.md）`

---

## ルール（原作解釈）

- 「このターンの間、自分のスピリットすべてBP+X」は、**使用後に召喚したスピリットにも乗る**
  （2026-07-31 利用者に確認）。`bpBuffAll` 系の約7枚が対象
- マジックの「対象」には**全体を含む効果も含まれる**（2026-07-30 利用者に確認）。
  これにより BS04-054 は実装価値がある（SPEC.md §1 BS04 に反映済み）

## 設計判断

- **「このターンの間」の継続化は `lentOnly`（貸与＝仮想発生源からのみ有効）で実装した**（2026-07-31 確定・
  2026-08-01 全15枚完了）。仕様は SPEC.md §「マジックが『このターンの間』継続効果を貸す」に昇格済み。
  却下した `requiresActivation`（発生源の個体にフラグを持たせる案）は**破壊時トリガーのカード3枚で原理的に
  成立しない**（発揮時点で発生源が場にいない）ため、この判断は再検討しないこと
- **`AuraDef.attackingOnly` を新設した**（2026-07-31）。既存の `battlingOnly` は「アタッカー**または**ブロッカー」なので、
  相手ターンに自分のブロッカーにも乗ってしまう。`attackingAll` は移行完了により廃止済み
- **state を受け取らない純粋述語（`instHasCost` / `instHasColor`）が読む継続効果は、走査ではなく
  `refreshLevelAsOverrides` の都度再構築で入れる**（2026-08-01）。signature 変更を全呼び出し箇所へ
  波及させないための定石（`colorsAsContinuous` / `alsoCostsContinuous` / `armorColorsGranted`）
- **未実装カードの「効果の読み替え層」は作らない**（2026-07-30）。3枚がそれぞれ別の接合点に当たるため、
  共通の器は「1枚しか乗らない抽象」になる。既存の接合点に最小の分岐を足す
- **BS05-079 スリーカードは入れない**（2026-07-30）。「数を数える」地点が条件・オーラ側に約30箇所あり、
  抜けが「無言で数値だけズレる」形で出る
- **ステップ誘発は解決の直前に「発生源のカード名」をログへ出す**（2026-08-03 確定）。
  アクション側のログは結果（「1枚ドローした」）しか残さず、どのカードの効果か追えなかった
  （UI担当からの指摘）。`fireStepTriggers` で1行出す形にしたので、
  **アクション個別にログを足して重複させないこと**
- **ソウルコアは案A（ポインタ）を採用**（2026-07-26）。詳細は `docs/design/SOULCORE.md`
- **多色は TargetFilter に統合しない**（2026-07-25）。規律（述語経由の色判定）だけ入れる。詳細は SPEC.md §1 BS05
- **覚醒UI（リザーブからの移動）はまとめて指定できるように変更しない**（2026-08-01 確定）。原作の「好きなだけ」とは異なるが、既存スピリットからの操作（1クリック＝1個）と操作感を統一するため
- **ネクサスのコア移動ボタンはスピリットと揃えて「ホバー時のみ表示」とする**（2026-08-01 確定）。ラッパー（`.nexus-slot`）全体をホバー判定とすることで押しにくさを解決した
- **代替コスト支払いの選択式化に伴い、サーバー側の `summon` に `discardHandIndices`、`setNexus` に `millPay` を追加した（いずれも省略可）**（2026-08-10 確定／出典: chatbox/archive/2026-08.md）

## 運用・インフラ

- **バグ報告の対戦ログはサーバーが付ける**（2026-07-30）。`attachedGame.logTail`（末尾200行）。
  クライアントの `clientContext` は `phase` / `turn` / `uiMode` / `lastError` の**4キーのみ**採用され、
  それ以外は `sanitizeClientContext` が破棄する

- **コミット時は `git add -A` を使わず、担当ファイルを明示的に指定する**（2026-08-02）。
  UI担当と実装担当で作業時間が重なりやすく、互いの未完成の変更を巻き込まないようにするため。
  UI担当は `git add public/ chatbox/` などを指定すること。

- **コミット前に `npm run typecheck` を必ず通す**（2026-08-03）。
  **`npm run build:client`（esbuild）は型チェックをしない**ため、型エラーがあってもビルドは成功し
  `public/dist/*.js` も生成される。実際に `public/src/deck.ts` の未定義関数呼び出し
  （`updateFilterVisuals`）がビルドを通過し、**デッキビルダーのフィルタリセットが実行時エラーで
  動かない状態でコミットされた**（`6bd28b6` → `340adee` で修正）。
  UI だけの変更でも `npm run typecheck` はリポジトリ全体を検査するので数秒で捕まる。

- **カードデータを触ったら `npm run validate:gaps` を通す**（2026-08-03）。
  cards.json は型検査の対象外なので「効果を書かなかったこと」は全緑をすり抜ける。
  実際にマジック48枚のメイン側とスピリット13枚が長期間見過ごされた。
  意図的に実装しない場合は `data/card-notes.json` に理由を書く（黙って落とさない）。

- **`clientContext` に `log` を入れてはいけない**（2026-07-31 実測）。`/api/bug-report` は
  `express.json({ limit: "32kb" })` のため、**約200行を超えた対戦で報告そのものが 413 で失敗する**
  （400行=62KB で再現）。しかも `log` は破棄されるので利得はゼロ

- **「効果を受けない」ルールは述語 `isEffectBlocked`（EffectModules）の中へ足す**（2026-08-01）。
  ガード地点は5ファイル18箇所に散っているため、述語を1つにまとめておけば以後のカードで再スイープが要らない
- **BS04-054 サンクの「対象に含む」判定**（2026-08-01）: 明示ターゲットが自身のとき、または
  明示ターゲット無し（全体効果）かつ自身が `action.filter` に合致するとき。
  BP3000以下の全体破壊に対して BP4000 のサンクは「対象に含む」ではないので絞り込まない

- **【神速】は自分・相手どちらのターンでもフラッシュタイミングで使える**（2026-08-01 利用者に確認）。
  「自分のアタックステップの間だけ」ではない。BS04-033 Lv2-3 / BS04-080 Lv2 の「【神速】の効果を変更する」は
  **タイミングではなくコアの取得元の話**で、基礎ルールの「支払いはリザーブのみ」に対して
  フィールド（ロングホーン上／自分のフィールドすべて）を取得元として追加する効果
- **「置くコア（指定Lvぶんのコア）」もフィールドのスピリット/ネクサス上のコアから取得できる**
  （2026-08-01 利用者に確認）。`paySources` の上限を `cost + maintain` へ広げ、フィールドから取った
  コアは先にコストへ充当し余りを置くコアへ回す形にした（SPEC.md §5 に仕様として記載）。
  これにより BS04-033 / BS04-080 の簡略化も解消した

## 実装で見つかった罠

- **`destroy` の pendingChoice 解決パスだけ免疫を再判定していなかった**（2026-08-01 に修正）。
  `coreRemove` / `returnToHand` は「候補列挙で除外済みでも targetInstanceId 経由では改めて判定する」形
  だったのに、`destroy` だけ素通しだった。**同種のガードは必ず全アクションで形を揃える**
- **免疫のガードは5ファイル18箇所に散っている**（SPEC.md §2「効果の無効化・読み替え」参照）。
  新しい「効果を受けない」を足すときは grep で全ヒットを機械確認すること

- **書き込むだけで誰も読まない状態フィールドが実際にあった**（2026-08-01）。`CardInstance.tempFamilies` は
  音鳥クルークが書き込むだけで、`spiritHasFamily` はそれを見ておらず**系統付与がゲームに一切影響していなかった**。
  smoke もフィールドを直接断定していたため緑のまま通っていた。**状態を足したら、それを読む述語を同時に指す**こと

## 調査の作法（同じ誤りを2回出したもの）

- **狭い grep で「不在」を推論しない。** 「ガードが無い」と書く前に、委譲先と共通述語まで追うこと。
  BS04-054（SPEC の記述を裏取りせず採用）と `exhaustOpponentToMatch`（ハンドラ本体だけ grep して
  委譲先の `pickEnemyCandidates` を見落とした）の2件が、いずれも誤った確定判断まで進んだ

## お知らせ欄（/api/changelog）

- **お知らせの実体は `data/announcements.json`**（2026-08-09）。`GET /api/changelog` がこれを新しい順50件、
  `{date, message, hash}[]` の形で返す（`message` は `[release:category] text` に組み立て、
  プレフィックスの解釈・除去はクライアントの `parseReleaseMessage` が担当。`hash` は空文字）。
  **当初はコミットメッセージの `[release]` プレフィックスを `git log --grep` で拾う実装だったが、
  同日中に廃止した**：文面の訂正に履歴の書き換えが必要で実際に直せなかったこと、
  `.git` の無いデプロイ環境では常に空になり本番で機能しないことが理由。書き方は CLAUDE.md 参照

## 共有ルール層（shared/）

- **サーバー検証とUIの発動可否は同じ関数を通す**（2026-08-09）。入れ替え召喚（BS07ブラックカラカロッサム）の
  判定は `shared/summon.ts` の `battleSwapSummonCheck` に一本化し、`RuleValidator.validateSummon` も
  UI向けの `canBattleSwapSummon` もそこを呼ぶ。二重実装は型エラーにならず実対戦でしか露見しないため
  （`activatableAbility` がサーバー検証より厳しくズレていた事故が実際にあった）。
  **コアの過不足だけは共有層で見ない**——支払い元（paySources）の配分検証はサーバー専用なので、
  共有層は必要数（`totalCores`）を返すところまでを契約とする
- **`shared/cost.ts` は `shared/rules.ts` を import している**。両方に依存する判定を rules.ts に置くと
  循環参照になるので、下流の別ファイル（`shared/summon.ts`）に分ける
- **`[カード名]` 表記はカード名の完全一致**（部分一致にしない）。"カラカロッサム" を部分一致にすると
  [ブラックカラカロッサム]自身に一致して自分と入れ替えられてしまう

## カードテキストの読み方

- **「コストを支払わずに」と書かれていなければ、召喚コストは通常どおり必要**（2026-08-09）。
  「〇〇を手札に戻すことで召喚する」は**追加コスト**であって召喚コストの免除ではない。
  ブラックカラカロッサムを一度コスト不要で実装して誤りだった

## プレイヤー選択（pendingChoice）の入れ方

- **「〜できる」と書かれた効果は、自動適用の簡略化のままにしない**（2026-08-15 ユーザー確認）。
  マジックの無償化・再発揮・対象の変更の3つを確認式へ移した。詳しくは
  `docs/design/COST_MODEL.md` §5 と `docs/design/CHOOSER_RULES.md` §1.5
- **他人の効果の対象を書き換える効果は、書き換える側の持ち主が選ぶ**（封印された魔導書Lv1）。
  マジックの使用者ではない。相手が使ったマジックにも効く

- **選択式に移すときは「候補2体以上のときだけ選ばせる」**（1体なら即確定・0体なら不発）。
  非対話モード（`interactiveTargets` が false ＝ smoke）では従来どおり自動選択を残すこと。
  応答は既存の `{ type: "resolveChoice", instanceId }` で、UI側の追加実装は要らない（2026-08-10）
- **`card-notes.json` の「選べません」は鵜呑みにしない**。王蛇の住処Lv2 は実測したら既に選択式で、
  記述のほうが古かった。着手前に `interactiveTargets = true` で1回動かして確かめる

## ログとイベントの書き方

- **効果で盤面が変わるログは「〈発生源カード名〉：〈対象〉が〜」の形にする**（2026-08-10 ユーザー要望）。
  「ファンタズマは疲労した。」だけでは何の効果か対戦者に追えない。【暴風】など
  キーワード由来なら「〈発生源〉の【暴風】：」まで出す
- **破壊以外でフィールドを離れるときも GameEvent を出す**（`returnToHand` / `returnToDeck`）。
  `pid` は戻された側の持ち主、`sourceName` は戻した効果の発生源（分かる経路のみ）
