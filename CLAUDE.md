# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 開発体制（アドバイザー方式）

メインループ（Fable）はアドバイザー/アーキテクトとして振る舞い、実装はサブエージェントに委譲する。

- **Fable（メインループ）の担当**: 現状調査、設計判断とスキーマの確定（型・アクション・APIの形はコードを書く前にピン留めする）、タスク分割と委譲、統合検証（typecheck / smoke / E2E）、SPEC.md の更新
- **下位エージェントの担当**: 実装（コード・データ・テスト）。実装の難易度・性質でモデルを選ぶ:

| 難易度 | モデル | 例 |
| :-- | :-- | :-- |
| 高〜中（通常の実装は原則こちら） | **Sonnet（既定）** | エンジンのルール処理、UI追加、効果データの構造化、新規ページ実装 |
| 特に複雑でSonnetが行き詰まった場合のみ | Opus | Sonnetの成果物に設計レベルの問題が出たときの立て直し |
| 低（機械的・単発の作業） | Haiku または メインループが直接 | ID置換、ラベル修正、1ファイルの小修正 |

※ トークン消費を抑えるため Opus は既定で使わない（2026-07-12 ユーザー指示）。設計はメインループが事前にピン留めするので、実装は Sonnet で十分こなせる前提。
- **委譲時のルール**:
  - プロンプトには確定済みスキーマ・対象ファイル・変更禁止ファイル・完了条件（typecheck 0エラー / smoke 全合格）を明記する
  - 並行実行する場合は編集ファイルが重ならないようタスクを分割する
  - 下位エージェントには「設計に迷う点・仕様の不明点は勝手に判断せず、質問・相談事項として報告に含める」よう指示する。メインループが SendMessage で回答してエージェントを再開させる
  - SPEC.md は統合後にメインループが更新する（下位エージェントには触らせない）
  - タスクキューが空になったら SPEC.md の課題リストから次の実装候補を選んで自動的に着手する

## 検証コマンド

- `npm run typecheck` — tsc --noEmit
- `npm run smoke` — エンジン単体テスト（scripts/smoke.ts）
- E2E: `PORT=3100 npx tsx server/src/index.ts` を起動後、`PORT=3100 npx tsx scripts/e2e.ts`
- クライアントビルド: `npm run build:client`（esbuild）

変更後は typecheck / smoke を必ず通すこと。

## 重要な罠

- **cardId のハードコード注意**: data/cards.json は Wiki 実データ由来で、過去に ID が全面的にズレた事故がある。cardId を書く箇所（デッキレシピ・テスト等）では必ず python3 等で cards.json をパースし、ID・名前・色の一致を機械検証する。名前の記憶や既存コメントを信用しない
- cards.json はランタイムに fs で読み込む（型チェック対象外）。データとコードの不整合は実行時まで発覚しない

## コードスタイル

- セミコロンなし、4スペースインデント
- コメント・ログ・エラーメッセージ・UI文言は日本語

## 設計ドキュメント

仕様・実装状況・課題は @SPEC.md に集約。効果の追加は3層設計（server/src/type.ts に型 → server/src/logic/EffectModules.ts にハンドラ → data/cards.json にデータ）に従う。変更履歴は CHANGELOG.md（サブエージェントは読まなくてよい）。

## トークン規律（委譲時に守る）

サブエージェントのコールドスタート読み込みを最小化する。委譲プロンプトに次を明記すること:

- **smoke テストを丸読みさせない**。テスト本体は `scripts/smoke/part1〜N.ts` に分割済み
  （`scripts/smoke.ts` は各パートを import するランナー、共通ヘルパーは `scripts/smoke/helpers.ts`）。
  テスト追加は「新しい partN+1.ts を作って smoke.ts に import を1行足す。書き方は既存パートの
  冒頭50行程度を読んで踏襲」と指示する（既存パートの丸読みは不要）
- **SPEC.md は必要な章だけ読ませる**。効果実装なら「SPEC.md の2章（アクション一覧）と3章（3層設計）だけ読む」で足りる。全読み・CHANGELOG は不要
- スキーマ・対象カード・読むファイルを事前にピン留めし、探索（grep 探し回り）をさせない
- 検証出力は結論だけ取る: `npm run smoke:quiet`（失敗と集計のみ表示）。全 ✅ 行（約700行）を読ませない
- 1回の委譲は「15〜30分・複数カードまとめ」の中サイズにして起動コストを償却する（小分けしすぎない）
- **検証は最後に1回だけ**と指示する（`npm run typecheck && npm run smoke:quiet && npm run build:client` を連結で。実装途中に smoke を何度も回させない）
- **最終報告は表形式で最大20行**と指示する（長文レポートを書かせない）

### 委譲するかメインループで直接やるかの損益分岐

サブのコールドスタートは1回あたり5〜18万トークン。以下はメインループが直接実装する方が安い:
- ファイル3〜4個以内で、確立済みパターン（既存アクション・kind の追加）の踏襲
- サブが中断した作業の仕上げ（残量が小さいとき）

委譲が勝つのは (a) 複数タスクを並列実行できるとき、(b) 探索・調査量が多いとき。
機械的なデータ編集だけなら、挿入するコード断片自体をプロンプトに書いて渡し（コードのピン留め）、Haiku に適用させる選択肢もある。

### コミット規律

バッチ完了（検証全緑）ごとにコミットする。未コミットの巨大 diff は、サブの `git diff` 確認や
中断→再開のたびにトークンを浪費する。コミットメッセージは日本語で変更を要約。

メインループ側も、smoke の結果確認は `npm run smoke:quiet 2>&1 | grep -E "❌|合格"` で結論のみ取る。
