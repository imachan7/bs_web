# 引き継ぎ事項（実装担当セッション向け）

作成: 2026-07-25 / 基準コミット: `6718d36`（ブランチ `gamestate`）

このファイルは**セッションを跨ぐときの立ち上がり用**。恒久的な仕様は [SPEC.md](./SPEC.md)、
進め方は [CLAUDE.md](./CLAUDE.md)、変更履歴は [CHANGELOG.md](./CHANGELOG.md) を参照。
**引き継ぎが完了して内容が古くなったら、このファイルは削除してよい。**

---

## 0. 最初に読む順番

1. **`chatbox.md` の末尾**（最重要）— 実装担当・設計担当・Gemini の**3者が並行作業**しており、
   ロック宣言と完了報告がすべてここに時系列で載る。**現在の可否判断はここが唯一の情報源**
2. `CLAUDE.md`（開発体制・検証コマンド・トークン規律）
3. `SPEC.md`（必要な章だけ。効果実装なら2章「アクション一覧」と3章「3層設計」）

---

## 1. ⚠️ エンジン側はロック中（最重要）

別セッション（設計担当）が **「TargetFilter 直交化」** に着手を宣言している。
**解除の連絡が `chatbox.md` に載るまで、次のファイルに触らないこと。**

- `server/src/logic/actions/`（全ファイル）
- `server/src/type.ts`（特に `EffectAction` 周辺）
- `shared/rules.ts`
- `data/cards.json`

**これは単なる衝突回避ではない**。フィルタ軸をどこまで直交化するかは一貫した設計判断が必要で、
並行して進めると「型は通るのに設計が矛盾したもの」ができる、というのが設計担当の説明。

### 触ってよいもの

- `public/` 配下・CSS・HTML（ただし UI は Gemini 担当。§3 参照）
- `DEPLOY.md` / `.github/workflows/` / Azure 周り
- `scripts/`（smoke のテスト追加など。ただしロック対象を import するテストは書けない）

### 背景（直交化が先である理由）

`cards.json` の全アクションフィールド117個のうち **28個（23%）が後付け**で、
同じ直交軸が action ごとに1弾ずつ足されているだけだった、という設計担当の実データ分析による。
実際、直近セッションで追加したものが多く含まれる（`destroy`/`exhaust`/`destroyExhausted` の
`costFilter`、`maxBpFromSelf`、`bpBuff` の `minSymbols`、`refreshOne` の `excludeSelf`、
`familyFilter` の OR 配列 など）。**BS05 を先にやると同じ後付けをさらに積むことになる。**

---

## 2. BS05（第五弾：皇騎）— 偵察のみ完了、未投入

**リポジトリには一切書いていない。** `data/cards.json` がロック対象のため保留中。

### 判明していること

- 全 **88枚**（通常 BS05-001〜084 ＋ Xレア X17〜X20、**欠番なし**）
- Wiki の取得元: `cmd=listcard&sdan=BS05&refer=第五弾：皇騎`（**2ページ構成**。`rowid=59632&pcnt1=1..2`）
  - ※ 弾名は推測せず Wiki のカードリスト索引で確認すること（「戦嵐」は第八弾、「皇騎」が第五弾）
- 色内訳: 赤14 / 紫14 / 緑14 / 白14 / 黄15 / 青15 ＋ **多色2枚**
- **新キーワードなし**。転召16件（うちコスト6以上/ボイドが4件）・覚醒5・粉砕3・神速3・呪撃2・光芒2・装甲4。
  すべて実装済み。**装甲の複数色指定**（赤/白、赤/紫/緑/白）が引き続き出る

### ⚠️ 多色カードが初登場（データモデル変更が必要）

- **BS05-X19 聖皇ジークフリーデン（赤・白）**
- **BS05-X20 大甲帝デスタウロス（紫・緑）**

現行の `CardData.color` は単一の `Color` 型。多色対応は `color: Color | Color[]` か
`colors: Color[]` の追加になるが、**色は軽減シンボル計算・装甲判定・`instHasColor`・
デッキビルダーのフィルタと広範囲に効く**。TargetFilter 直交化と同時に設計するのが妥当か、
判断は設計担当に委ねてある（`chatbox.md` で共有済み）。**勝手に決めて進めないこと。**

### パース済みデータは失われている

パース結果はセッション固有のスクラッチパッドにしか無く、**新セッションでは参照できない**。
再取得が必要（curl 2回＋パーサー実行で数分）。BS01〜BS04 のパーサーで踏んだ罠は次のとおり:

- `refer` パラメータ必須（無いと「パラメータ不正」ページが返る）
- **タグ除去 → エンティティ展開の順**で処理する（先に展開すると `&lt;1&gt;`（Lv1のコア数）が
  タグ扱いで消える）
- タイプ行に `(禁止カード)` / `(制限カード<1>)` が付くことがある（後者は `limitCount` フィールドへ）
- レアリティはカード名の後ろの単独大文字（`C,R` のような複数表記あり）
- 軽減シンボルは「コスト(軽減数)」表記から自色×個数で再構成（BS02〜BS04 は全カード単色だったが、
  **BS05 は多色があるためこの前提が崩れる**。パーサーの色処理は要修正）
- cards.json 投入前に **python3 で cardId・名前・色を機械検証**すること（過去にID全面ズレの事故あり）

---

## 3. Gemini（UI担当）への依頼が進行中 — 完了報告待ち

**デッキビルダーのフィルタ拡張**を依頼済み（`chatbox.md` に仕様あり）。

- **弾フィルタ**（`cardId` 先頭4文字で判定。**実在する弾を動的生成**する方針＝BS05追加時に自動で増える）
- **キーワードフィルタ**（`effects` の `kind:"keyword"` の有無で判定。効果文の参照だけのカードは除外）
- **系統フィルタ**（54種あるためチップ不可。`<select>` か `<datalist>` を選んでもらう形）

完了報告が来たら、実装担当が検証して `gamestate` → `main` にマージする（**main へのマージで自動デプロイ**）。

**Gemini の worktree が古い**: `/Users/imachan/develop/bs_web-ui` が `58a8b1e`（リファクタ前）のまま。
着手前に `git merge gamestate` が必要と2回伝えてある。

---

## 4. インフラ・運用（直近で整備済み）

### CI/CD が機能するようになった

`main` への push で **typecheck → smoke → E2E → デプロイ → デプロイ後ヘルスチェック** が自動実行される
（`.github/workflows/azure-deploy.yml`）。PR #4 で初回成功を確認済み。

- **開発は `gamestate`、リリースは `main` へマージ**という運用
- 検証が1つでも落ちればデプロイされない
- **デプロイ後ヘルスチェック**は、ビルドもテストも通るのに本番だけ起動しない事故を検知するために追加した
  （実際に `shared/` の同梱漏れで起こりうる状態だった）

### 手動デプロイ（Actions が使えないときの退避路）

DEPLOY.md 5.5 に手順あり。**要点は2つ**:

- **`shared/` を必ず zip に含める**（リファクタで `server/src` が実行時に `shared/*` を import するようになった。
  含め忘れると本番が `MODULE_NOT_FOUND` で起動しない）
- **zip は毎回削除してから作る**（同名の古い zip が残っていて「デプロイしたのに反映されない」事故が実際に発生した）

### 本番の状態（2026-07-25 時点で実測確認済み）

- https://bs-web.azurewebsites.net — `/health` 応答あり
- カード521枚・**BS04 の構造化 91/97**・`main.js` がローカルとバイト一致
- バグ報告フォーム稼働中（`/bugreport.html`）。**報告の閲覧**は
  `/api/bug-reports?key=7f1fcd6ebd6d6c2e4fbb1053`（Azure の app settings `BUG_REPORT_KEY`）

---

## 5. 現在のコード状態

| 項目 | 値 |
| :-- | :-- |
| ブランチ | `gamestate`（`origin/gamestate` より3コミット先行・要 push） |
| `main` との差 | 64コミット |
| 検証 | typecheck 0エラー / **smoke 2,146件 全合格** / build:client 成功 / E2E 合格 |
| BS01〜BS03 | 効果文を持つ全カード構造化済み |
| BS04 | 効果文持ち97枚中 **91枚**構造化（残6枚は理由付きで見送り。SPEC.md に記載） |

### 直近セッションでの主な変更

- BS04 の効果実装（転召・ダブルシンボル・手元ゾーン・召喚レベル指定 ほか）
- **大規模リファクタリング完了**（REFACTOR.md の計画。同ファイルは完了により削除済み）
  - `shared/` にルール判定を一本化（サーバー/クライアントの二重実装を根絶）
  - `resolveAction` 3,189行・105 case → 54行のディスパッチャ ＋ `actions/` 7モジュール
  - `ActionRegistry` により**網羅性チェックを型で維持**している。ハンドラ追加時はここに載せること
- この過程で**実バグ5件**を解消（詳細は CHANGELOG.md と `scripts/smoke/part56.ts`）

---

## 6. 次にやること（ロック解除後）

1. 設計担当の TargetFilter 直交化の完了を待つ（`chatbox.md`）
2. **多色カードのデータモデル**をどうするか設計担当と合意する
3. BS05 のデータ再取得 → パース → `cards.json` 投入（**機械検証必須**）
4. キーワードは全て実装済みなので、構造化は色別バッチから始められる
5. Gemini のデッキビルダー対応が完了したら検証 → `main` へマージ（自動デプロイ）
