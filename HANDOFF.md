# 引き継ぎ

**この文書は「次のセッションが知らないと困ること」だけを書く。80行を超えたら削る。**
**ただし §1 の「進行中バッチの設計」は一時的に超えてよい**（バッチ完了時に手順書へ移して消すため）。

| 書くもの | 書かないもの（＝どこにあるか） |
| :-- | :-- |
| 未決の論点、合意待ちの変更、まだ文書化していない罠 | 何をやったか → `git log` |
| 進行中の作業の**次の一手**（1行＋リンク） | なぜそう決めたか → `docs/design/` の手順書 |
| 確定したがまだ手順書に移していないスキーマ | 検証コマンド・手順書の地図 → `CLAUDE.md` |

**書くのは設計を確定した時点で、実装より先にコミットする**（詳細は CLAUDE.md）。実装が終わったら手順書へ移して消す。

**手順書へ1行として移したら、ここからは消す。** 二重に持つと必ず片方が古くなる
（2026-08-27 に、この文書が10日ぶんの実装とズレて「本線」を間違って指していた事故があった）。

---

## 1. いまの本線と次の一手

**BS12「星座編 第三弾：月の咆哮」は91枚すべて投入済み（2026-09-07 完了）。**
確定した解釈18件と全バッチの器は [BS12_PLAN.md](./docs/design/BS12_PLAN.md) §1 と §5〜§8 にある。

**BS11-065 満天の牧草地／BS11-X05 魔導双神ジェミナイズ Lv2-3の未実装2枚は完了（2026-09-07）。**
確定した規則は COST_MODEL.md §8（「支払った」の判定）に移した。smoke part295 / part296。
**これで未実装の節は全弾でゼロになった**（BS02-063 は禁止カードのため対象外）。

**簡略化5件も原作どおりに直した（2026-09-07）。`card-notes.json` の `simplified` は0件になり、
残るのは禁止カード BS02-063 の `partial` 1件だけ**（実装しない方針）。


**次の一手: BS13「星座編 第四弾：星空の王者」97枚を入れる。** 計画は
[BS13_PLAN.md](./docs/design/BS13_PLAN.md)。**新しいキーワードは1つも無い**弾で、
主題は【不死】の条件指定・スピリット状態のブレイヴへの干渉・色の打ち消し・本来のコスト。

### バッチ1a（赤・前半8枚）の確定スキーマ（2026-09-07 ユーザー確認。完了したら手順書へ移して消す）

対象: BS13-001〜008。**赤は17枚あるので前半/後半に分ける**（BS12 赤と同じ形）。

| 器 | 形 | 対象 |
| :-- | :-- | :-- |
| C | **本来のコスト**（軽減前）で判定する軸。既存 `instMatchesCostFilter` は軽減後・付与コストを見るので別物。カード静的な `cost` を見る | 003 |
| D | 『アタック時』に自分のスピリット状態のブレイヴ1体と合体し、**その後**そのスピリットの『合体アタック時』効果を発揮させる。**合体しなければ発揮しない**（2026-09-07 ユーザー確認。「その後」＝前後関係） | 007 |
| J | 「BP合計(系統：◯のBP合計)まで、相手のスピリットを好きなだけ破壊できる」。既存の「BP合計◯まで」の**系統合計**版カウンタ | 008 |
| M | `familyFilter` の **AND** 版（「系統：「地竜」と系統：「竜人」**両方**を持つ」）。既存 `FamilyFilter = string \| string[]` は配列＝OR なので新しい形が要る | 061（後半） |
| N | 【超覚醒】の支払い元を「自分のスピリット上」から**リザーブ**に変える継続付与 | 002 |
| O | 「分離できず、お互い、その上のコアは取り除けない」。**「お互い」はコア除去にだけかかる**（2026-09-07 ユーザー確認）。分離禁止の軸は既存に無いので新設 | 005 |

既存で足りるもの: 001＝バニラ ／ 004＝【激突】＋`onBattleEnd` の自己破壊コスト＋`summonFromHandFree`（familyFilter 神星）／
006 Lv1-3＝`fieldEvent` `anySpiritAttacked` `subjectSide:"opponent"` ＋ `destroy filter:{maxBp:4000}`、
Lv2-3＝バッチ0の `symbolAddGrant`（条件「スピリット状態のブレイヴがいる間」）／
005【合体時】Lv3 の「“〜”という効果を与える」＝BS12-068 で作った `effectEntryGrant`（フラッシュ【超覚醒】を配る）／
008 Lv2＝『ブロックされたとき』＋「地竜1体を手札に戻すことで回復」（既存 `refreshSelf` のコスト軸）

### バッチ1b（赤・後半9枚）の見立て

対象: 049 / 050 / 061 / 062 / 073 / 074 / X01 / X006 / X011。**1a の完了後に確定させる。**
深い器は E（ブレイヴ同士の合体。049）／G（他のブレイヴの『合体アタック時』を借りて発揮。049。
**借りた効果の「このスピリット」は発揮する側＝イリテバンを指す**＝2026-09-07 ユーザー確認）／
「ブレイヴ2つまでと合体できる」（X01。既存に上限の軸が無い）／
**継続で実効BPを12000にする**（X011。**場にいる間ずっと**＝2026-09-07 ユーザー確認。
BS12 の `setBattleBpFixed` はバトル中だけなので継続版が要る）。

BS12 が終わったあとの残課題は [REMAINING_WORK.md](./docs/design/REMAINING_WORK.md)（検証の穴80件＋計測点の無い kind 10種）。

BS11 は91枚すべて投入済み。

BS10（121枚）とブレイヴの段階1〜7は完了済み（[BRAVE.md](./docs/design/BRAVE.md) §9）。
BS11 で確定した規則は BRAVE.md §12.5.1〜§12.5.5 に移してある。

---

## 2. 未決（答えが出たら手順書へ1行移して、ここから消す）

**いまは無い**（2026-09-02 に PROCEDURES_AUDIT §5 の Q2/Q3/Q4/Q6 と COST_MODEL の保留がすべて決着した）。

---

## 3. ⚠️ 挙動が変わった既存カード（ユーザーの合意待ち）

破壊待機状態の導入で5枚の挙動が変わった。いずれも仕様（[TIMING_CHART.md](./docs/design/TIMING_CHART.md) §1.5）の
帰結でテストにも固定してあるが、**うち3枚は効果が強くなる方向**なので、意図と違えば戻すこと。

| カード | 変化 | 強弱 |
| :-- | :-- | :-- |
| 宝石の獣カーバルク | 『破壊時』の「自分のフィールドにいる想獣1体につき」に**自分自身が入る** | 強くなる |
| 戦闘獣ジャッカー | 「破壊されたネクサスを戻す」が**待機解除**になり、コアもレベルもそのまま残る | 強くなる |
| 兵隊アントマン | 『破壊時』の召喚で**自分のコアはまだ使えず**、**自分のシンボルは軽減に数える** | 一長一短 |
| 盾精ラングリーズ／神鳴る霊峰 | 破壊されたコアを、リザーブ経由でなく**待機中の本体から直接**移す | 同等 |
| `returnSelfToHand` | トラッシュからでなく**破壊待機状態から**手札へ戻る | 同等 |

指定アタックの手順を直したことで、既存の `canDirectAttack` 持ち5枚
（イリュージョナ／牛霊スモゥグ／オルカリア／シンクロニシティ／BS11-X02 滅神星龍ダークヴルム・ノヴァ）の
挙動も変わる。**指定された側の『ブロック時』効果が発揮するようになり、【装甲】持ちは指定できなくなる**
（2026-09-06 ユーザー指示による修正なので合意済み）。

---

## 4. 決着済み（蒸し返さないこと）

- **再開スタック方式を採る**（ジェネレータ化はしない）。理由は [RESUME_STACK.md](./docs/design/RESUME_STACK.md) §8
- **`allowSuspend` と `pendingReviveConfirms` は消さない。** この2つは「その場で聞く」と
  「恩恵の後に聞く」の使い分けそのもの（RESUME_STACK.md §7）
- **発動確認の抑止はやらない。** 成立しない任意コスト効果でも確認は出る

---

## 5. 間欠的に踏む罠（文書化先が無いのでここに置く）

- **`createGame(seed, …)` の seed は名前だけで、シャッフルは `Math.random()`**（`GameState.ts` の `shuffle`）。
  **デッキの中身に依存するテストは間欠的に落ちる。** 必要なカードは自分で山札の先頭へ置くこと。
  smoke が1〜2件落ちたら、まず**同じコマンドを再実行**して再現するか見る
- **再開スタックは `act()` の解決ループでしか消化されない。** 束を積むだけでは `pendingChoice` が立たず、
  呼び出し元からは「何も起きなかった」ように見えて誘発が放置される
