# 引き継ぎ（2026-08-16 セッション終了時点）

作業内容そのものは `git log` で足りるので、**次のセッションが知らないと困ることだけ**を書く。

---

## 0. まず読む順番

1. この文書
2. CLAUDE.md の「⚠️ ゲームルールの解釈は、実装前に設計を示して確認する」節
3. 下の §2「ルールの情報はどこにあるか」の表から、**着手する作業に関係するものだけ**

---

## 1. いま何をやっているか

### 大きな流れ（2026-08-14 更新）

**「効果の割り込み（中断・再開）を、あとから足せる形にする」**土台整備を進め、
その土台の上で**第九弾「超星」（BS09）の取り込み**に入ったところ。

```
土台1〜8（完了） → バトル解決の再開可能化（完了） → BS09 設計調査（完了）
  → 【不死】実装（完了） → 破壊待機状態の実装（完了）
  → **BS09 全91枚の構造化（完了）** → ★いまここ
```

**第九弾「超星」は91枚すべて取り込み済み**（`data/cards/BS09.json`。staging に残っているものは無い）。
`npm run coverage:effects` も回し、BS09 の効果エントリで実行実績0のものは**0件**。
`data/announcements.json` にお知らせも出してある。

### 2026-08-15 にやったこと（「〜できる」の自動適用をやめた）

効果文が「〜**できる**」なのに自動で適用していた3つを、**確認式**へ移した。
どれも UI側の追加実装は不要（`kind:"option"` の既存表示で受けられる）。
これで chatbox の「はい/いいえ系4枚」の依頼（2026-08-10-1641）は**4枚すべて完了**。

| 何を | 変わったこと |
| :-- | :-- |
| 封印された魔導書Lv1 の対象変更 | **持ち主に3択**（変更しない／相手のみ／自分のみ）。従来は持ち主に有利な側へ自動固定 |
| マジックの無償化（5枚） | 使用のたびに**2択**。あえて払えば1枚きりの枠が残る |
| 再発揮（イスフィール） | 1回目の解決後に**2択**。発揮しなければ枠が残る |

確定した規則は `docs/design/CHOOSER_RULES.md` §1.5 と `docs/design/COST_MODEL.md` §5 に記録済み。
**非対話（テスト・自動解決）では従来どおり**なので、既存 smoke は無改変で通っている。

**残件**：封印された魔導書の対象変更のうち、**スピリットイリュージョン（BS02-111）だけ未対応**。
「このターンの間、指定した色のスピリットすべてを最高Lvとして扱う」は**ターン中持続する継続効果**で、
マジック解決中しか生きない `magicSideDecision` では絞れない。やるなら仮想発生源
（`lentChoiceColor` を持っているところ）に残す側の pid も持たせることになる。

**次にやる候補**:

1. **ブレイヴ（合体スピリット）**。破壊待機の手順3「ブレイヴを残すか選ぶ」もここで入る
2. **カバレッジの積み残し**（BS01〜08 ぶん）。`npm run coverage:effects` が
   実行実績0の継続効果を数十件挙げている。BS09 は潰したが古い弾は手つかず。
   2026-08-15 に【強襲】【氷壁】【聖命】【不死】の計測点を足したので、
   いま残っている8件は**本物のテストの穴**（smoke がそのカードを一度も使っていない）
3. 上の残件（スピリットイリュージョン）
4. UI担当への依頼（chatbox `2026-08-14-1900`）の返答待ち：探偵ペンタンの「相手の手札を裏のまま選ぶ」UI

### 2026-08-16 にやったこと（品質保証と、そこで見つかった実バグ）

ユーザー方針：**ブレイヴは、今のシステムがほぼバグ無しと保証できてから**着手する。
そのため「書いてあるのに一度も動いていない効果」を潰す作業を進めた。

| 指標 | 開始時 | 現在 |
| :-- | :-- | :-- |
| 場に出ているのに一度も適用されていない効果 | 105件 | **1件** |
| action を持つ効果の実行率 | 96.1% | **99.9%** |
| 継続効果の実行率 | 87.9% | **100.0%** |
| smoke | 7027件 | **7371件** |

**その過程で実バグを4件見つけて直した**（いずれも回帰テストつき）:

1. **「他の相手を破壊する」で選んだ相手が破壊されず、同じ選択が立ち続ける**（実プレイで進行不能）。
   選択待ちに渡す action に `excludeTarget` が残っていた（part193）
2. **バウンスがまとめ戻しになっていなかった**。1体戻すごとに「戻ったとき」が割り込み、
   本来手札に戻るはずの相手が破壊されていた（part194／TIMING_CHART.md §1.6）
3. **【激突】と強制ブロックが装甲を素通りしていた**（ユーザー指摘）。
   ブロック可否しか見ておらず耐性判定を通していなかった（part195／TIMING_CHART.md §1.7）
4. **ネクサスの `magicBuffBonus` が一度も働いていなかった**。発生源をスピリットだけ走査していたため、
   BS06-085 混迷する魔法実験場の「BPを+するマジックをさらに+2000」が無効だった（part200）

**新しい検証の道具**:

- **`npm run fuzz`** … ランダムデッキで自動対戦し、例外・進行不能・保存則違反・デッドロックだけを見る。
  シード固定で再現できる。定型検証には入れない（150戦・26000手で異常0を確認済み）
- **デバッグ用の盤面差し替え**（`POST /api/debug/setup`）… 盤面・手札・相手のカードを用意して
  実際に動かすための入口。**ローカル実行時だけ有効**。対戦画面は既存のものをそのまま使う
  （2つのタブで同じルームに入って対戦を始め、組んだ盤面を流し込む）。
  **UI（盤面エディタ画面）は UI担当へ依頼する**のが残件

**カードごとの個別テストで 1件まで減らした**（part195〜200）。**継続効果は100%到達**。
残る1件は **BS09-X37 終焉の騎神ラグナ・ロック**で、【転召】の条件になる「鉄騎皇イグドラシル」が
**未収録**のため条件を満たす盤面を作れない（そのカードが入るまで検証不能）。

途中で判明して直したもの:

| 見つかったこと | 直し方 |
| :-- | :-- |
| BS08-065 五行寺院に **`selfMode:"source"` が抜けていた** | 効果がアタックした相手側で解決され「自分の獣頭にコアを置く」が一度も働いていなかった。データを修正（同じ形のゼンマイ平原は最初から持っている） |
| magicRestriction の `costLimitAll` は **`hasMagicCostLock` という別関数**を通る | 計測点を足した。実装は正しく、動作は part199・200 で確認済みだった |
| constraintGrant の `colorFromChosen` は **別の push** を通る | 同上（BS09-081 サマーソルトターン） |

個別テストを書くと**カードごとに違う「働かせるための条件」**が分かる。part196〜200 のコミットメッセージに
その一覧を残してある（絞り込みは系統だけでない／マジックは貸与が要る／
`condition` 付きだけが対象を見る／色条件は eventColors・コスト条件は eventInfo.costs を見る、など）。

### ブランチと検証の状態

- 作業ブランチ **`worktree-battle-resolve-resume`**（origin に push 済み）。
  `resume-stack` からの早送りで、メインの作業ツリーは `resume-stack` が checked out のまま。
  取り込むときは `git merge --ff-only worktree-battle-resolve-resume`
- **全検証が緑**（2026-08-16 時点）：`typecheck` / `validate:cards`（**997枚**）/ `validate:notes` /
  `validate:gaps` / `smoke`（**7305件**）/ `build:client` / E2E / `fuzz`（50戦）/ 対話モード診断

### 2026-08-14 に直した既存カードの挙動（挙動が変わっている）

| 何を | 影響 |
| :-- | :-- |
| **「Lv1コスト」＝Lv1に必要なコア数**に直した（従来はカードの召喚コスト） | BS08-059 聖なる柱状彫刻の保護が弱くなる |
| **対象の付け替え**（`magicTargetRedirect`）を**スピリットの効果にも**効かせた | BS05-040 プリンセス・スノーホワイトが正しく守れるようになる |
| **【不死】の召喚が「自分のスピリットが召喚されたとき」を起こす**ようにした | 【不死】召喚がフィールド誘発を踏むようになる |
| `coverage:effects` の import 追随漏れを直した | 2026-08-10 の関数移設以降、計測が ReferenceError で落ちていた |

### 直近6コミットでやったこと

| コミット | 中身 |
| :-- | :-- |
| 土台8 | バトル解決（＞５〜＞７）を `runBattleStep` の11ステップに割り、`battleResolve` フレームで再入できるようにした。相打ちは1つの破壊バッチにまとめた |
| BS09 設計調査 | staging 91枚を既存効果文と機械比較。**新規は14行だけ**。新しい器が要るのは6件 |
| 【不死】実装 | BS09 の新キーワード。トラッシュのカードを、味方の破壊を引き金に**通常コストを払って**召喚 |
| 破壊待機状態（仕様） | ユーザーから受け取った＞６の中身を `TIMING_CHART.md §1.5` に一次資料として記録 |
| 破壊待機状態（実装） | `destroySpirit` を「破壊待機 → 誘発 → トラッシュ → コア」に組み替えた |
| 【不死】を窓の中へ | 【不死】の解決を破壊待機の窓の中に移し、ネクサスにも破壊待機状態を入れた |

---

## 2. ルールの情報はどこにあるか（最重要）

### ⚠️ 毎セッション必ずやること（2026-08-14 ユーザー指示）

**効果を実装する前に [CONJUNCTION.md](./docs/design/CONJUNCTION.md) の早見表を見る。**
そのうえで、**接続詞の解釈が結果を変えるカードは、実装前にユーザーへ「こう読みました」と確認する。**
セッションが切り替わっても、これは毎回やること（前のセッションで確認済みかどうかに関わらず、
新しく触るカードについては聞く）。

**バッチ単位で、実装前に解釈一覧を出してから書く**（2026-08-14 ユーザー指示）。
「既存の器で書けるもの／新しい器が要るもの」を表にして、判断が割れる点を先に潰す。

### ⚠️ 大前提

**ゲームルールの正解はリポジトリに書かれていない。**
カード906枚の「効果の語彙」は SPEC.md に厚く書いてあるが、ゲームの「**手順**」は誰も決めていない。
推測で実装すると**間違ったまま smoke が全緑になる**（テストが実装の写しになる）。

したがって:

1. 迷ったら**まず下の表の手順書を読む**
2. 書いていなければ**ユーザーに聞く**（「私はこう解釈しました → これでいいですか」の形で）
3. **得た答えは必ず `docs/design/` の手順書に書く**。チャットに残すだけだと同じ規則を何度も発見し直す

### 手順書の地図

| 知りたいこと | 見る場所 |
| :-- | :-- |
| **バトル中の解決順**（＞０〜＞７のタイミング表）・**同時発揮の一般則**・**破壊待機状態** | **`docs/design/TIMING_CHART.md`** |
| 効果文の**接続詞**（その後／さらに／そうしたとき／ことで／この効果発揮後／このとき） | `docs/design/CONJUNCTION.md` |
| 「Aすることで、Bする」の**コストの一般則** | `docs/design/COST_MODEL.md` |
| 効果文の主語が「**相手は**」のとき、誰が選ぶか | `docs/design/CHOOSER_RULES.md` |
| **中断された側をどう書くか**（再開スタック・挿入順）・召喚と【転召】の手順 | `docs/design/RESUME_STACK.md` |
| どの層なら中断できるか・3つの実装パターン | `docs/design/INTERRUPTION_POINTS.md` |
| **BS09 の取り込み計画**・【不死】の仕様 | `docs/design/BS09_PLAN.md` |
| 効果＝アクションの語彙（180種）と3層設計 | `SPEC.md` の §2 と §3 だけ（**全読み禁止。86KB**） |
| 残りの実装漏れを進める手引き | `docs/design/EFFECT_GAPS_PLAYBOOK.md` |
| 先行設計（BRAVE / ULTIMATE / SOULCORE / BURST） | `docs/design/` の各ファイル。着手時のみ |

### 今セッションでユーザーに確認して確定した規則（すべて手順書に転記済み）

| 規則 | 書いた場所 |
| :-- | :-- |
| 同時発揮は A（継続効果）→ B（タイミング・条件）の順。**それぞれの中はターンプレイヤーが順番を決める** | TIMING_CHART.md §0 |
| アタックステップのタイミング表（＞０〜＞７） | TIMING_CHART.md §1 |
| **破壊待機状態**の定義と性質（場に残る／コアも乗ったまま／効果の対象になる／誘発も継続効果も働く／疲労も回復もできない／さらに破壊されない） | TIMING_CHART.md §1.5 |
| 効果文の「**戻る**」は**トラッシュからではなく破壊待機状態から**。後に「残る」へ記述変更された | TIMING_CHART.md §1.5 |
| 破壊待機状態のスピリットは**【転召】の生贄にできる** | TIMING_CHART.md §1.5 |
| BP比較（＞５）は破壊処理（＞６）より先。**敗者が「フィールドに残る」で生き残っても『BPを比べ〜破壊したとき』は発揮する** | TIMING_CHART.md §2 |
| 【呪撃】【光芒】は＞７。＞６で残った個体も【呪撃】の対象になる | TIMING_CHART.md §2 |
| 相打ちの解決順の粒度は**体ごと**（ターンプレイヤーが選ぶ） | TIMING_CHART.md §2 |
| 【不死】は**通常のコストを支払う**／確認は＞６のその場／「フィールドに残る」と同時発揮で順番が結果を変える | BS09_PLAN.md §3 |
| BS09-044 ハマ・ドリュアスの「BPを比べずにブロックされなかったものとして扱う」は**ライフに通る** | BS09_PLAN.md §4 |

---

## 3. ⚠️ 挙動が変わった既存カード（ユーザーの合意待ち）

破壊待機状態の導入で、**5枚の挙動が変わった**。いずれも仕様（TIMING_CHART.md §1.5）の帰結で、
テストで固定してあるが、**うち3枚は効果が強くなる方向**なので意図と違えば戻すこと。

| カード | 変化 | 強弱 |
| :-- | :-- | :-- |
| 宝石の獣カーバルク | 『破壊時』の「自分のフィールドにいる想獣1体につき」に**自分自身が入る** | 強くなる |
| 戦闘獣ジャッカー | 「破壊されたネクサスをフィールドに戻す」が**待機解除**になり、コアもレベルもそのまま残る（旧：Lv1・コア0で置き直し） | 強くなる |
| 兵隊アントマン | 『破壊時』の召喚で**自分のコアはまだ使えず**、一方で**自分のシンボルは軽減に数える** | 一長一短 |
| 盾精ラングリーズ／神鳴る霊峰 | 破壊されたコアを、リザーブ経由でなく**待機中の本体から直接**移す | 同等 |
| returnSelfToHand（破壊時に手札へ戻る） | トラッシュからでなく**破壊待機状態から**手札へ | 同等 |

---

## 4. BS09（超星）の状況

- **91枚すべて `data/cards/BS09.json` へ取り込み済み**（`data/staging/BS09.json` は取り込み元として残っているだけ）。
  計画・新しい器の一覧は `docs/design/BS09_PLAN.md`
- `npm run coverage:effects` も回してあり、BS09 の効果エントリで実行実績0のものは**0件**
- **第九弾は異色軽減を導入している**（91枚中37枚が自色＋他色1色の軽減）。
  軽減の計算は色ごとに数えるので engine 側の変更は不要だったが、
  `validate:cards` の「軽減は自色の範囲に収まるはず」という検査を弱めてある

---

## 5. 決着済み（蒸し返さないこと）

- **再開スタック方式を採る**（ジェネレータ化はしない）。理由は RESUME_STACK.md §8
- **`allowSuspend` と `pendingReviveConfirms` は消さない。** かつて「移行が終わったら消す」と
  書いていたが、調べた結果この2つは**①と②の使い分けそのもの**だった
  （① 別効果の「破壊したとき」＝その場で聞く／② 「破壊することで」＝恩恵の後に聞く）。RESUME_STACK.md §7
- **発動確認の抑止はやらない。** 成立しない任意コスト効果でも確認は出る
- **UIは UI担当（Gemini）の担当。`public/` を書き換えない。** 必要なら chatbox で依頼する

---

## 6. 未決・保留

- **「Aすることで、Bを2つする」で B が1つしか満たせないとき**（COST_MODEL.md §1）。
  該当2枚（剣王獣ビャク・ガロウLv2／カイザーアトラス皇帝Lv2）。
  現状の挙動（いる分だけ処理してコストは払う）を smoke part178 で固定してある。
  正解が分かったらしきい値1か所（`>= 1` → `>= count`）を切り替える
- **ブレイヴ（合体スピリット）が未実装**。破壊待機の手順3「ブレイヴを残すか選ぶ」はそのときに入る
- **破壊待機中のアタック・ブロックの可否**は未対応。破壊待機中にアタック宣言は起きないので実害なし

---

## 7. 検証の定型

```
npm run typecheck && npm run validate:cards && npm run validate:notes && npm run validate:gaps && npm run smoke:quiet && npm run build:client
```

- **`npm run smoke:interactive` は診断用**（定型には入れない）。見るのは
  「例外なし・すべての選択待ちが解消・保存則を維持・中断ガードに引っかからない」の4つ。
  アサーション失敗は選択結果が変わるため想定内
- E2E: `PORT=3100 npx tsx server/src/index.ts` を起動後、`PORT=3100 npx tsx scripts/e2e.ts`
- **弾がまるごと入り終わったら `npm run coverage:effects` を1回**（遅いので定型には入れない）
