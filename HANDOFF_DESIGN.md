# 引き継ぎ（設計担当セッション向け）

作成: 2026-07-26 / 基準コミット: `6c31e0d`（ブランチ `gamestate`）

このファイルは**設計担当セッションを引き継ぐとき**の立ち上がり用。
実装担当セッション向けの引き継ぎは [HANDOFF.md](./HANDOFF.md)（別物なので混同しないこと）。
**引き継ぎが完了して内容が古くなったら削除してよい。**

---

## 0. 最初に読む順番

1. **`chatbox.md` の末尾**（最重要）— 実装担当・設計担当・Gemini の**3者が並行作業**しており、
   ロック宣言・完了報告・依頼がすべて時系列で載る。**現在の可否判断はここが唯一の情報源**
2. このファイル（役割と進行中の論点）
3. `CLAUDE.md`（開発体制・検証コマンド・トークン規律）
4. 必要になった設計文書だけ（下の一覧）

---

## 1. この役割は何をするか

**設計・レビュー担当**。コードを大量に書くより、**横断的な設計判断・他セッションのレビュー・
基盤の整備**を持つ。実装担当（BS05以降のカード構造化）と Gemini（UI）とは担当が分かれている。

| セッション | 担当 | 作業場所 |
| :-- | :-- | :-- |
| **設計担当（これ）** | 横断設計・レビュー・テスト基盤・検証スクリプト | `/Users/imachan/develop/bs_web`（`gamestate`） |
| 実装担当 | カード構造化・エンジン実装 | 同上（同じツリーを共有） |
| Gemini | UI のみ（`public/` 配下） | `/Users/imachan/develop/bs_web-ui`（`feature/ui-improvements`） |

### ⚠️ 実装担当と**同じ作業ツリーを共有している**

`git status` に自分が触っていない変更が出るのは**普通のこと**（相手の作業中分）。

- **コミットは必ずファイルを明示して `git add`**。`git commit -a` は禁止
  （このリポジトリでは過去に、他セッションの未コミット変更を巻き込む事故が実際に起きている）
- 相手が触っているファイルには手を出さない。必要なら chatbox で依頼する
- smoke が一時的に赤くても、相手の編集途中である可能性がある。**数分後に再実行して確認する**

---

## 2. 現在の状態（`6c31e0d` 時点）

- 作業ツリー: **クリーン**
- `npm run typecheck`: 0エラー
- `npm run smoke:quiet`: **2415件 全合格**
- `npm run validate:cards`: 問題なし（609枚）
- smoke パート: **1〜65 と 70 が使用済み**。**設計担当は 70番台**を使う約束
  （実装担当のサブは 63以降、Gemini は UI のみでパートを使わない）

### この役割が入れたもの（すべてコミット済み）

| 成果 | コミット |
| :-- | :-- |
| TargetFilter 直交化 第1段階（絞り込み軸の共通型化） | `a525ca6` |
| 混色軽減バグの修正（実データで再現→修正→`part59`） | `be97938` |
| 実対戦経路（`interactiveTargets=true`）のテスト（`part60`） | `5f86c87` |
| `cards.json` スキーマ検証の常設化（`part61` / `npm run validate:cards`） | `8f399e8` |
| 第2段階の前提テスト（`part70`、15枚） | `d66a528` |
| `lendSelfThisTurn` の検査追加＋走査分類の訂正 | `25edc77` |
| 設計文書 3本（下記） | `02525b3` / `1216530` / `1388808` |

### 書いた設計文書

| 文書 | 状態 |
| :-- | :-- |
| [TURN_EFFECT_SOURCES.md](./TURN_EFFECT_SOURCES.md) | **実装済み**（実装担当が `ff120db`）。文書は分類基準の訂正入り |
| [BRAVE.md](./BRAVE.md) | 設計のみ。次年度の新カードタイプ |
| [BURST.md](./BURST.md) | 設計のみ。次年度の新機構 |
| [MULTICOLOR.md](./MULTICOLOR.md) | 実装担当が作成・実装済み |

---

## 3. 未着手・保留中のタスク

### 3.1 TargetFilter 第2段階（最優先候補）

**前提は埋まっている**（`part70` で未カバー15枚を押さえた）。やることは:

1. `cards.json` の旧フィールド（`maxBp` / `keywordFilter` / `familyFilter` …）を新形式 `filter` へ移行（57箇所・44枚）
2. `server/src/type.ts` から旧フィールドを削除 → tsc が漏れを全部指摘してくれる
3. `server/src/logic/actions/filter.ts` の互換層 `legacyToSpec()` を削除

**着手条件**: 実装担当が `type.ts` / `actions/` を触っていないこと。**chatbox で宣言してから始める**。

補足: `bpBuff.minSymbols` は `matchesTarget` ではなく `pickBpBuffTarget`（`EffectModules.ts`）側にある。
第1段階で意図的に未変換にした箇所なので、第2段階で寄せるかは別途判断する。

### 3.2 その他

- `BRAVE.md` / `BURST.md` は**実カードのデータが手に入ってから**未確定事項を埋める
  （それぞれの文書の「未確定」節を参照）
- `BURST.md` は Wiki の記述に不確かな点が2つある（発動後のカードの行き先・空打ちの可否）

---

## 4. この役割で確立した「効く」手法

**引き継いだら、これは続けてほしい。** どれも実際にバグを見つけている。

### 4.1 変異テスト（最重要）

**「smoke が全部緑」はテストが効いている証拠にならない。** 条件分岐に入らないテストを書いても緑になる。

判定を1箇所わざと壊して、**期待どおり落ちるか**を確認する。復元を忘れないこと。

```bash
cp shared/rules.ts /tmp/bak.ts
# 判定を1行コメントアウト
npx tsx scripts/smoke.ts 2>&1 | grep '❌'
cp /tmp/bak.ts shared/rules.ts
```

実績:
- `part70` で `minSymbols` を壊したら**落ちなかった** → 実経路が `pickBpBuffTarget` 側だと判明（テストは正しかったが、私の理解が誤っていた）
- 実装担当もこの手法を採用し、`effectSources()` の検証に使っている

### 4.2 cardId は必ず機械照合する

**名前の記憶で選ぶと外す。** `CLAUDE.md` にも書かれている罠。

実績: `BS01-020 翼刃竜スティラノドン` を神速持ちとしてテストに使おうとしたが、
実際は `awaken`（覚醒）だった。`python3` で `cards.json` を引いて判明。

```bash
python3 -c "
import json
cards={c['cardId']:c for c in json.load(open('data/cards.json'))}
c=cards['BS01-020']; print(c['name'], c['colors'], [e.get('keyword') for e in c['effects'] if e.get('kind')=='keyword'])
"
```

### 4.3 「実装されているが誰も通っていない経路」を疑う

実績:
- `turnStartResumeStep`（ターン開始処理の中断・再開）はテスト**0件**だった → `part60` で追加
- **【激突】**は第一弾から実装済みだったが保持カードが0枚で動作実績がなかった →
  指摘したところ、実装担当が通してみて**詰みバグを発見・修正**した（`8a8af03`）

### 4.4 走査の A/B 分類（横断設計の判断基準）

「フィールドを舐める」コードには2種類ある。**関数単位ではなく、その走査が何を問うているかで決める。**

| 問うていること | 分類 | フィールドにない発生源（仮想発生源・将来の合体中ブレイヴ） |
| :-- | :-- | :-- |
| 誰が継続効果を出しているか | A | 含める |
| 盤面に何が存在するか（数える・色・シンボル） | B | **含めない** |

混同すると「集計が意味を潰す」事故になる。実績が2件ある。

- 混色軽減バグ（`be97938`）: 軽減シンボルを色ごとに突き合わせず集合として数えていた
- `checkAuraCondition.hasOwnColor`（`6c31e0d` で差し戻し）: マジックを貸しただけで
  「場に赤のカードがある」が成立するところだった

### 4.5 挙動保存の根拠は「既存テストを1行も変えずに通す」

リファクタや内部変更のとき、**既存 smoke を書き換えずに通ることが唯一の客観的根拠**。
テストの期待値を書き換えたくなったら、それは挙動を変えている。

---

## 5. chatbox の運用

- 書式: `## [送り手→受け手] 日付 — 見出し` ＋ 本文 ＋ `状態:` 行
- **末尾に追記**（上書き禁止）
- 自分の名義は `設計担当`。他者が別名義で書いた設計が並んだことがあるので、
  **設計が2案並んだら統合して「これだけを見てください」と明示する**（前例: `35ef1f5`）
- 作業前に「どのファイルを触るか」を宣言する。ロックを依頼したら**解除も必ず連絡する**

### 監視の設定

chatbox の更新を検知する Monitor を仕掛けていた（追記があったときだけ通知）。
新セッションでも同じものを張るとよい。

```bash
cd /Users/imachan/develop/bs_web
pat='^## |^→ '
prev=$(grep -Ec "$pat" chatbox.md 2>/dev/null || echo 0)
while true; do
  sleep 60
  cur=$(grep -Ec "$pat" chatbox.md 2>/dev/null || echo 0)
  if [ "$cur" -gt "$prev" ]; then grep -E "$pat" chatbox.md | tail -n $((cur - prev)); prev=$cur
  elif [ "$cur" -lt "$prev" ]; then prev=$cur; fi
done
```

---

## 6. 他セッションの状況（`6c31e0d` 時点）

- **実装担当**: BS05 の効果構造化を色別バッチで進行中（赤紫・緑白・黄青は完了）。
  `TURN_EFFECT_SOURCES` の実装も完了済み
- **Gemini**: 多色カードの表示対応まで完了。`public/` 配下のみ。
  worktree は `4de9eeb`。**`feature/ui-improvements` → `gamestate` のマージは実装担当が担当**

### Gemini との付き合い方（実績にもとづく注意）

- 報告が実態と食い違ったことがある（「実装完了」が未コミット・古いベース・中核が no-op だった）。
  **完了報告は git で裏を取る**（コミットの有無・ベース・差分の中身）
- 担当範囲（`public/` のみ）を越えたことがある。越えていたら chatbox で指摘してよい
- 直近（多色表示対応）は**報告どおり正確**だった。頭ごなしに疑うのではなく、毎回確認する

---

## 7. 検証コマンド

```bash
npm run typecheck        # tsc --noEmit
npm run smoke:quiet      # エンジン単体テスト（結論だけ表示）
npm run validate:cards   # cards.json のスキーマ検証（この役割で新設）
npm run build:client     # esbuild
```

`main` にマージされると GitHub Actions が本番へ**自動デプロイ**する
（typecheck → smoke → E2E が全部通らないとデプロイされない）。
手動 zip デプロイでは **`shared/` の同梱が必須**（漏れると本番が MODULE_NOT_FOUND で起動しない）。
