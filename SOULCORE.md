# 設計: ソウルコア（1個だけ特別なコア）

- 作成: 2026-07-26
- **この文書は設計のみ。実装は未着手**
- 出典: [バトスピ Wiki「ソウルコア」](https://batspi.com/index.php?%E3%82%BD%E3%82%A6%E3%83%AB%E3%82%B3%E3%82%A2)
- 関連: [ULTIMATE.md](./ULTIMATE.md)（`minLevelCores` を共有する）、[SPEC.md](./SPEC.md)、
  [HANDOFF_DESIGN.md](./HANDOFF_DESIGN.md) §4.4（走査の A/B 分類）

---

## 0. 結論（先に6行）

1. **コアは数値のまま。ソウルコアは「1個だけの居場所ポインタ」で表す。**
   `PlayerState.soulCore: SoulCoreLocation` を足すだけで、**コアを加減算している 72箇所は無改修**
2. コア識別（`cores: Core[]`）への一般化は**やってはいけない**。
   識別が必要なのは 1個だけで、配列化すると 72箇所すべてを書き換えることになる
3. ソウルコアが**行けない場所は「ライフ」と「ボイド」の2つだけ**。
   出口は実測で **ボイド 6箇所・ライフ 1箇所**で、**うち1つは既に共通ヘルパー**
   （`removeCoresToVoid`）。ここに関所を置けば足りる
4. 初期リザーブは現行と同数の4個（`INITIAL_RESERVE = 4`）のまま、
   **内訳が「通常3＋ソウル1」になる**。2026-04-11 のルール改訂で**使用は強制**なので選択制は実装しない
5. ポインタ方式の弱点は**数値との二重管理**。防御は**不変条件チェック1つ**
   （`assertSoulCoreConsistent`）に集約し、smoke の共通ヘルパーで全テストに掛ける
6. **既存の【転召】と衝突する。** 「コアすべてをボイドへ」はソウルコアだけ残るため、
   **転召したスピリットがコア1個で生き残る**（現行コードは全部動く前提）

---

## 1. ルール（出典から確定した事実）

### 1.1 基本

- **各プレイヤーが1個だけ所有する特別なコア**（烈火伝編から）
- ゲーム開始時、**通常のコア3個とソウルコア1個**をリザーブに置いて開始する
- **2026-04-11 から、使用は任意ではなく強制**（それ以前は通常コア4個で始めてもよかった）
- 基本的には通常のコアと同じように使える

### 1.2 行けない場所

- 《封印》等の**ソウルコアを対象とした効果でなければ**、
  **ライフ / ボイド / 創界神 / 創界石には移動できない**
- ライフ/ボイドに置く効果では、**可能な限り通常のコアを移動する**。
  **通常のコアがない場合は不発**
- 複数のコアをまとめてボイドに送る効果では、**ソウルコアのみがリザーブに残る**

### 1.3 持ち主が選べるもの

- 相手の効果でコアがリザーブやトラッシュに移動するとき、
  **ソウルコアを含むかどうかはスピリット/アルティメットの持ち主が選べる**
- ライフ/ボイドへ移動する効果の場合は、**含めることはできない**（1.2 が優先）

### 1.4 メインステップの 1:1 交換

- メインステップのコア移動時、**スピリット/アルティメット2体間で
  通常のコア1個とソウルコア1個を交換できる**
- このとき**コアが1個だけでも「同時に移動している」ため消滅しない**
- 通常のコアの移動を同時に行っている扱いなので、
  **夢魔の寝所（コア増加の検知）では両方のスピリットが疲労する**
- コアを取り除けない効果が掛かっているときは、交換もできない

### 1.5 ソウルコア固有のデメリットルール（★重要）

> ソウルコアをスピリット/アルティメット上から移動させる効果により、
> **その効果を発揮したスピリット自身が消滅してしまう場合は、その効果の発揮自体ができない。**

- 維持コアを1個しか乗せていないスピリットが、自身から1コスト支払って発揮する効果は
  **ソウルコアでは発揮できない**
- ソウルコアの使用指定がない効果でも、**結果的にソウルコアを動かすことになり消滅する場合は発揮できない**

### 1.6 その他

- 【ソウルドライブ】や一部の《煌臨》によって「**除外**」される
  （ボイドではなく、プレイシートの外に置く）
- 《封印》する効果によりライフに置くことができる（＝《封印中》効果の前提）

---

## 2. ⭐ データモデル: 「1個だけのポインタ」

```ts
export type SoulCoreLocation =
    | { zone: "reserve" }
    | { zone: "trash" }
    | { zone: "instance"; instanceId: string }   // スピリット/アルティメット/ネクサスの上
    | { zone: "life" }                           // 《封印》のみ（v1範囲外）
    | { zone: "removed" }                        // 除外（【ソウルドライブ】。v1範囲外）
```

```ts
// PlayerState
soulCore: SoulCoreLocation
```

### 2.1 なぜ完全なコア識別にしないのか

コアを数値で持っている箇所は **72箇所**（実測）。

| ファイル | 箇所 |
| :-- | --: |
| `server/src/logic/actions/cores.ts` | 28 |
| `server/src/logic/EffectModules.ts` | 19 |
| `server/src/logic/GameEngine.ts` | 16 |
| `actions/destroy.ts` / `PhaseManager.ts` / `actions/battleFlow.ts` / `actions/grant.ts` | 9 |

`cores: number` を `cores: Core[]` にすると、**この72箇所すべてが配列操作になる**。
一方で識別が必要な対象は**ゲーム全体で2個**（各プレイヤー1個）しかない。

> **必要のない一般化で全走査を壊す**のは、混色軽減バグ（`be97938`）の裏返しの失敗。
> あちらは「区別すべきものを混ぜた」、こちらは「混ぜてよいものを区別した」で、
> どちらも走査側に意味のないコストを載せる。

### 2.2 ポインタ方式の弱点と、その唯一の防御

数値（`reserve` / `inst.cores` / `trashCores`）と**ポインタが二重管理になる**。
「リザーブのコアが0個なのにポインタが `reserve` を指している」状態が作れてしまう。

**防御は不変条件チェック1つに集約する。**

```ts
// scripts/smoke/helpers.ts に置き、act() の直後に毎回掛ける
export function assertSoulCoreConsistent(state: GameState): void {
    for (const pid of ["p1", "p2"] as PlayerId[]) {
        const loc = state.players[pid].soulCore
        // ポインタの指す先に、ソウルコアぶんの実体（1個以上）があること
        // reserve → reserve >= 1 / trash → trashCores >= 1 / instance → 実在して cores >= 1
    }
}
```

**全 smoke に自動で掛かる形にする**のが要点（個別テストで書くと掛け忘れる）。
これは「実装されているが誰も通っていない経路」（`HANDOFF_DESIGN.md` §4.3）の予防でもある。

### 2.3 公開情報なのでマスクしない

ソウルコアは盤上で区別できる形で置かれる（＝**両者に見えている**）。
`viewFor` では**手元ゾーンと同じ扱い**で、両者分の `soulCore` をそのまま配信する。

### 2.4 対案（案B）: 型を変えてコンパイラに全箇所を列挙させる

実装担当から出た対案。`MULTICOLOR.md` の案A（`color: Color` → `colors: Color[]`）と同じ発想で、
`cores: number` を識別子つきの表現に変え、**tsc に 72箇所すべてを指摘させる**。

| | 案A（ポインタ。本文書の推奨） | 案B（型を変えて列挙） |
| :-- | :-- | :-- |
| 改修量 | 関所7＋支払い＋交換＋転召 ≒ **10箇所** | **72箇所**（＋クライアント） |
| 漏れの検出 | **コンパイラは何も言わない**（弱点） | tsc が全部指摘する（強み） |
| 走査側の複雑さ | 変わらない（`inst.cores` は数のまま） | すべて配列操作になる。**コア数を数えるだけの箇所にも配列が漏れる** |
| 多色化との違い | — | 多色は**全カードが色を持つ**ので全箇所に意味があった。**ソウルコアはゲーム中2個だけ** |

**判断: 案A（ポインタ）を採る。** 理由は、識別が必要な対象が2個しかないのに
72箇所へ配列を持ち込むと、**「コアを数えるだけの箇所」に意味のないコストが載る**こと。
これは混色軽減バグ（区別すべきものを混ぜた）の裏返しの失敗＝**混ぜてよいものを区別した**にあたる。

**ただし対案の指摘は正しい。** 案Aの弱点は「コンパイラが助けてくれない」ことで、
これは §2.2 の不変条件チェックだけでは**書き漏らしそのものは見つけられない**
（不整合が起きたときに初めて落ちる）。そこで**列挙の利益だけを別に取る**:

> **段1で `PlayerState.reserve` → `reserveCores` の改名を単独コミットで入れる**（挙動は完全に不変）。
> tsc が全箇所を指摘するので、**「この箇所はソウルコアの判断が必要か」を1回だけ全部見る**ことができる。
> 改名を戻す必要はなく、以後は名前が「ここはコアの数である」ことを示す。

これは「型を変えて列挙させる」の**安価な代用**であり、案Bの利点（全箇所を必ず見る）を
案Aのコスト（配列化しない）で得るためのもの。**採否は実装時に判断してよい**
（改名だけで1コミット・レビュー1回ぶんのコストはかかる）。

### 2.5 明示指定でしか動かない（設計上の簡略化）

案Aが 10箇所で済むのは、次の**簡略化を明示的に置く**からである。

> **ソウルコアは「ソウルコアを指定した操作」でしか動かない。**
> 効果によるコア移動（リザーブ→スピリット、スピリット→リザーブ 等）では、
> ソウルコアは**常に元の場所に留まる**（＝通常のコアが動く）。

出典では、相手の効果でトラッシュ/リザーブへ動くときに「含めるかは持ち主が選べる」（§1.3）ので、
これは**持ち主に有利な側へ寄せた決定的簡略化**であり、ルール違反の方向には倒れない。
ソウルコアが動くのは次の4つだけになる:

1. コスト支払いで明示指定したとき（§4）
2. メインステップの 1:1 交換（§5）
3. 「ソウルコアを置く」効果（§6）
4. 《封印》・除外（v1範囲外）

**この簡略化を SPEC.md の簡略化一覧に必ず記録すること。** 忠実化（選択を出す）は後から足せる。

---

## 3. 関所（ソウルコアを通さない場所）

### 3.1 出口は実測7箇所（うち1つは既に共通ヘルパー）

| 行き先 | 場所 | 内容 |
| :-- | :-- | :-- |
| ボイド | `EffectModules.ts:1066` | **`removeCoresToVoid`（既存の共通ヘルパー）**。`coreRemove dest:"void"` と `coreRemoveMulti` の2箇所から呼ばれる |
| ボイド | `EffectModules.ts:361` | 【転召】の `dest:"void"`（§3.3 で衝突する） |
| ボイド | `actions/cores.ts:388` / `:405` | `coreToVoidOwn`（トラッシュのコアから優先、次に実効BP最小のスピリット） |
| ボイド | `actions/cores.ts:587` | `voidCoresAndMillByCost`（BS05 マジックスパナ。**コアすべて**） |
| ボイド | `actions/grant.ts:235` | `costReserveToVoid`（リザーブからの支払い） |
| ライフ | `actions/cores.ts:977` | `lifeCharge`（`player.life += amount`） |

**`removeCoresToVoid` が既にある**のは追い風で、ここに関所を置くだけで2経路が同時に守られる。
残り5箇所を同じ形に寄せるのが §3.2。

> なお `removeCoresToVoid` は内部で `lv1Cores` を使って維持コア割れを判定している。
> [ULTIMATE.md](./ULTIMATE.md) §4 の `minLevelCores` 移行対象（24箇所）に含まれる。

### 3.2 共通ヘルパーに寄せる

```ts
// 「通常のコアを可能な限り動かす。通常コアが足りなければその分は不発」
export function moveNormalCores(
    state: GameState, pid: PlayerId,
    from: { zone: "reserve" } | { zone: "trash" } | { zone: "instance"; inst: CardInstance },
    to: "void" | "life",
    count: number,
): number   // 実際に動いた数を返す
```

**ソウルコア未実装の状態でこのヘルパーへ寄せる改修だけを先に入れれば、挙動は完全に等価**
（ポインタが常に「動かせる」ならヘルパーは素の加減算と同じ）。
`millDeck` が実破棄枚数を返すようにした改修（BS03 粉砕連動バッチ）と同じ形で、
**戻り値を「実際に動いた数」にする**のが後続の効果（「ボイドに置いた数ぶん〜」）に効く。

### 3.3 ⚠️ 既存の【転召】と衝突する

【転召】は「自分のコストN以上のスピリット1体の**上のコアすべて**を指定先に置く」。
指定先が**ボイド**のカード（BS04-X13/X15、BS05 の Xレア4枚）では:

- ソウルコアはボイドへ行けない → **ソウルコアだけがスピリット上に残る**
- 結果、**そのスピリットはコア1個で生き残る**（現行コードは「全部動かす→維持コア割れで消滅」前提）

**転召の消滅判定は必ず「実際に動いた数」を見る形に直す**こと。
ここは smoke を書くべき最優先の1件（転召持ちは実カードで8枚以上あり、テストも既にある）。

---

## 4. コスト支払い

### 4.1 トラッシュへ行くので支払いには使える

コスト支払いの行き先はトラッシュ（`payCost` は支払い元のコアをトラッシュへ送る）なので、
**ソウルコアはコストに使える**。「ソウルコアで支払うと追加効果」が成立するのはこのため。

```ts
// 既存の PaySource（スピリット/ネクサス上のコアでの支払い）に1つ足す
export interface PaySource {
    instanceId: string
    count: number
    includeSoulCore?: boolean   // この支払いにソウルコアを含める（そのインスタンス上にある場合のみ）
}
// リザーブからの支払いにソウルコアを使う場合は、アクション側に paySoulCoreFromReserve?: boolean
```

### 4.2 「ソウルコアで支払ったとき」誘発

既存の `fieldEvent` に1つ足すだけでよい（新機構は不要）。

```ts
event: "ownPaidWithSoulCore"
```

### 4.3 デメリットルール（§1.5）— 支払い前チェック1つ

```ts
// そのインスタンス上のソウルコアを1個動かしたら維持コアを割るか？
// 割るなら「効果の発揮自体ができない」（コストを払わせず、効果も発揮させない）
export function canMoveSoulCoreFrom(card: CardData, inst: CardInstance): boolean {
    return inst.cores - 1 >= minLevelCores(card)
}
```

`minLevelCores` は [ULTIMATE.md](./ULTIMATE.md) §4 で新設する関数と**同一のものを使う**
（現行 `lv1Cores` のままだと、アルティメット導入後に Lv3始まりのカードで誤判定する）。
**2機構の唯一の接点なので、`minLevelCores` は先に入れておくとどちらからも使える。**

> 「発揮自体ができない」＝コストを払ってから不発、ではない。
> `validateActivateAbility` 側で弾く（既存の起動能力のコスト検証と同じ位置）。

---

## 5. メインステップの 1:1 交換

```ts
| { type: "swapSoulCore"; fromInstanceId: string; toInstanceId: string }
```

- `from` にソウルコアがあり、`to` に**通常コアが1個以上**あること
- **消滅判定を挟まない**（「同時に移動している」ため。§1.4）。
  ここを既存の `moveCore` の実装（1個ずつ動かして毎回維持コアを検査する）に載せると
  **1個のスピリットが一瞬コア0になって消滅する**ので、専用アクションにする
- **`exhaustOnManualCoreAdd`（夢魔の寝所）は両方のスピリットに発火させる**
  （出典に明記あり。既存機構への接続を忘れないこと）
- コアを取り除けない効果が掛かっているときは交換不可（§1.4）

---

## 6. 「ソウルコアがある間」「ソウルコアを置く」

どちらも**既存の器に1軸足すだけ**で済む。新しい解決機構は要らない。

| 効果文 | 器 | 足すもの |
| :-- | :-- | :-- |
| 「このスピリットにソウルコアがある間〜」 | `AuraCondition` / `triggered.condition` | `selfHasSoulCore: true` |
| 「自分のフィールドにソウルコアがある間〜」 | 同上 | `ownFieldHasSoulCore: true` |
| 「ソウルコアを置くことで〜」 | `activated.cost` | `soulCoreFromReserve: true` |
| 「ソウルコアをトラッシュに置くことで〜」 | `activated.cost` | `soulCoreToTrash: true` |

**注意（分類B）**: 「ソウルコアがある間」は**盤面の存在を問う条件**なので、
`turnVirtualInstances`（マジックが貸した仮想発生源）を数えてはいけない
（`TURN_EFFECT_SOURCES.md` §1 の `hasOwnColor` と同型の罠）。

---

## 7. 段階移行

各段で `npm run typecheck && npm run smoke:quiet && npm run validate:cards` が緑であること。
**既存 smoke を書き換えずに通ることを挙動保存の根拠にする。**

| 段 | 内容 | 検証 |
| --: | :-- | :-- |
| 0 | `minLevelCores` の導入（ULTIMATE.md §4 と共通。どちらの機構から入れてもよい） | 既存が無変更で通る |
| 1 | ボイド/ライフへの出口8箇所を `moveNormalCores` に寄せる（**まだソウルコアは無い**） | 既存が無変更で通る＝挙動等価の根拠 |
| 2 | `PlayerState.soulCore` の新設＋初期リザーブの内訳変更＋`assertSoulCoreConsistent` を smoke 全体に掛ける | 既存が無変更で通る（不変条件が破れないこと） |
| 3 | 関所の実装（ライフ/ボイドへは通常コア優先・不足分は不発） | ソウルコアがボイドに行かない smoke |
| 4 | **【転召】の消滅判定を「実際に動いた数」基準へ**（§3.3） | 転召でコア1個残って生き残る smoke |
| 5 | コスト支払い（§4）＋デメリットルール（§4.3） | ソウルコア払いで消滅する効果が**発揮できない**こと |
| 6 | 1:1 交換（§5） | 交換で消滅しないこと・夢魔の寝所で両方疲労すること |
| 7 | 条件・誘発（§6）＋実カードの構造化 | カードごとの smoke |
| 8 | クライアント（ソウルコアの見た目・交換UI）※ Gemini 担当 | 目視 |

**段3・段5には必ず変異テストを掛ける**（`HANDOFF_DESIGN.md` §4.1）。
関所のガードを1行外して「ソウルコアがボイドへ行ってしまう」テストが**落ちること**を確認する。
落ちなければ、そのテストは条件分岐に入っていない。

---

## 8. `validate-cards.ts` に追加すべき検査

- `activated.cost.soulCoreFromReserve` を持つ効果の `levels` が `null` でないこと
- 「ソウルコアを置く」コストと `reserveToTrash` の**同時指定がない**こと（二重支払いの防止）
- `selfHasSoulCore` 条件を持つ効果が、スピリット/アルティメット/ネクサスのカードにのみ載っていること
  （マジックに載せても発生源が場に残らないため無意味＝`lendSelfThisTurn` の検査と同型）

---

## 9. v1では見送る（簡略化として SPEC.md に記録すること）

| 項目 | 判断 |
| :-- | :-- |
| 相手効果でトラッシュ/リザーブに動くときの「含めるか」の選択（§1.3） | **含めない（通常コア優先）を既定**にする。`pendingChoice` を出すのは後回し。ソウルコアを守る方向の簡略化なので、プレイヤー不利にはならない |
| 《封印》（ソウルコアをライフに置く） | 別機構（封印中効果・ライフ上のコアの区別）。`zone: "life"` は型に用意だけしておく |
| 「除外」（【ソウルドライブ】） | `zone: "removed"` は型に用意だけしておく。アルティメット側の実装が前提 |
| 創界神 / 創界石 | ゾーン自体が未実装 |
| ソウルコアの代用品・見た目の区別 | クライアント側の表示課題（Gemini 担当） |
