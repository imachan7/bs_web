# 残っている作業（2026-09-07 棚卸し）

BS12 の91枚が入り終わった時点で、「未実装」「簡略化」「検証の穴」を数え直したもの。
**枚数を手で持たない。** 下の各節は必ずコマンドの出力から数え直すこと（手書きの表は必ず古くなる）。

| 何を知りたいか | 回すコマンド |
| :-- | :-- |
| 効果の実装漏れ | `npm run gaps:report` |
| 未実装・簡略化の一覧 | `data/card-notes.json`（`npm run validate:notes` で整合を検査） |
| 書いたのに一度も発火していない効果 | `npm run coverage:effects`（遅い。弾が入り終わったときに回す） |
| 対戦者が選ぶべき場面を実装が決めていないか | `npm run audit:choices`（誤検出を含む） |

---

## 1. いまの実態（2026-09-07 の実測）

- **効果の実装漏れ: 実質ゼロ。** `validate:gaps` のベースラインは 0 件。`gaps:report` の
  残り1件は BS02-063（禁止カードのため実装しない方針。card-notes.json に登録済み）
- **未実装（機能として動かない）: 0件**（§2。BS11-065／BS11-X05 は2026-09-07に実装済み）
- **簡略化（動くが原作と違う）: 5件**（§3）。いずれも対戦は成立する
- **検証の穴: 80件＋α**（§4）。**ここが一番大きく、一番危ない**
- **`audit:choices` は誤検出のみ**（2026-09-07 に全件トリアージ済み。§5）

---

## 2. 未実装 ― 0件（2026-09-07 に完了）

BS11-065 満天の牧草地「手札を破棄できない」／BS11-X05 魔導双神ジェミナイズ Lv2-3
「コストを支払ってマジックを使用したとき、もう1枚を無償で使用できる（ターン2回）」の
2枚を実装した。確定した規則は COST_MODEL.md §8。smoke part295 / part296。

※ BS02-063 冥犬ケルル・ベロスは partial のまま残るが、**禁止カードのためデッキに入らない**。実装しない方針（数に入れない）。

## 3. 簡略化 ― 5件

**いずれも「このままでよい」と判断済み**（`card-notes.json` に対戦者向けの注記あり）。
直すとしたら費用対効果は低い。原作どおりにする価値が出たときだけ着手する。

| カード | 何を簡略化しているか |
| :-- | :-- |
| BS04-081 | 「ターンに1回ブロックされない」を、印がついた次の1回のアタックで使い切る |
| BS08-062 | 「シンボルを白3つにする」を、素の白1つに白2つを追加する形で同等の軽減量にしている |
| BS11-036 | 手元に置けるマジックカードの上限（本来3枚）が無い |
| BS11-042 | 【粉砕】でデッキを1枚以上破棄すればコアを得られる（本来はスピリットカードが出たときのみ） |
| BS12-078 | デッキ横の5個のコアをボイドから出さず、エンドステップの回数だけ数える |

## 4. 検証の穴 ― ここが本丸

`coverage:effects` の実測（2026-09-07）。**未発火の効果は BS10〜BS12 に集中し、BS09 以前はゼロ。**
新しく入れたデータを smoke が一度も通していない層で、**ここでしか見つからないバグがある**
（実績: `returnSelfToHand` の実行実績0、【激突】と `turnStartResumeStep` の実バグ）。

| 層 | 件数 | 中身 |
| :-- | --: | :-- |
| 場に出ているのに一度も適用されていない効果 | **80件** | triggered 18 / fieldEvent 16 / keyword 13 / ほか |
| 一度も実行されていない action | 2種 | `destroyOwnFreelyThenDraw`（BS12-052）／`negateContinuousMagicByName`（BS12-049） |
| テストが手で組んだ action でしか実行されていない（**カードデータ経由が未検証**） | 7種 | `grantHostUnblockableThisTurn` / `markSkipNextRefresh` / `mutualKeepChoice` / `refreshWhenBlockedByChosenColorThisTurn` / `revealTopCastMagicFreeOrHand` / `revealTopSummonFreeOrHand` / `setBattleBpFixed` |
| 継続効果で**計測点が無い** kind | 10種 | `altSummonFromHand` / `armorEffectiveGrant` / `braveImmuneGrant` / `braveStatsAs` / `destroyAsMaxLevelGrant` / `effectEntryGrant` / `symbolAddGrant` / `trashImmunity` / `trashNameAs` / `trashSymbolReduction` |

**最後の行が一番たちが悪い。** 計測点が無い＝「発火したか分からない」ので、
`coverage` の数字が良く見えても実態が分からない。BS12 で足した器が多く含まれる。
`scripts/coverage-effects.ts` に差し込み先を足すのが先。

⚠️ ブレイヴ経由のキーワード／`globalConstraint` は、実際に動いていても「未実行」と出続けることがある
（CLAUDE.md の既知の限界）。「未実行」が出たらまず smoke で実際に発火させて確かめる。

## 5. 自動選択の棚卸し

`audit:choices` は32種を挙げるが、**2026-09-07 に全件トリアージして誤検出のみと確認した**。
内訳は PROCEDURES_AUDIT.md §4 の誤検出パターンどおり:

- 「すべて」を対象にする効果（BS10-061 / BS10-108 / BS11-049 / BS12-006）
- 選択の語が**別の節**のもの（BS12-008 の指定アタック、BS10-X01 の【超覚醒】、BS11-X02）
- `chooseActionMode` 越しで静的に追えないもの（**BS12-049 は既に選ばせている**）

「選ぶと書いてあるなら選ばせる」の一般則（PROCEDURES_AUDIT §5）は満たせている。

## 6. 文書のズレ

- **SPEC.md §5 の枚数表が古い**（`simplified` 28件・`partial` 1件と書いてあるが、実態は5件・3件）。
  枚数を書くのをやめて `card-notes.json` を指すだけにするのがよい

---

## 7. 着手の順（提案）

1. **`coverage-effects.ts` に計測点を足す**（未計測10種）。これをやらないと以降の数字が信用できない
2. **未発火80件のうち、実バグが隠れていそうなものから smoke を足す**。弾ごと（BS12 → BS11 → BS10）に区切る
3. **カードデータ経由が未検証の7種**にデータ経由のテストを足す
4. ~~未実装2件~~（2026-09-07 完了。BS11-065／BS11-X05）
5. SPEC.md §5 の枚数表を消す（§6）

**1〜3 は「動いているはずのものが本当に動くか」の確認**で、実プレイの事故を減らす。
