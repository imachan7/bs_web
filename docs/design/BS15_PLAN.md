# BS15「覇王編 第2弾：黄金の大地」実装計画

**全91種**（コモン46 / アンコモン18 / レア12 / マスターレア8 / Xレア7。X014 超星覇龍ヤマトヴルム・ノヴァを含む）。
データは `data/staging/BS15.json`（2026-09-16 取り込み・ID の機械検証済み）。効果文なし（バニラ）は6枚。
BS14 と同じ覇王編なので、**【バースト】が主題として続く**（機構は BS14 で実装済み。[BS14_PLAN.md](./BS14_PLAN.md) §2 が確定スキーマ）。

**⚠️ この文書は着手前の計画である。カードデータをまだ取り込んでいないため、
下の「解釈の確認」「バッチ分割」は枚数・カード名が入っていない枠だけの状態。**
データを取り込んだ時点で §2 と §4 を埋めてから実装に入ること。

---

## 0. 着手前に片付けるもの（BS15 より先）

弾を足すと**監査の未判定が増えて見えなくなる**。BS14 でそれが起きた
（S6・S7 が「残0件」から 12件・9件に戻り、実バグ5件が埋もれていた）。
**同じことを繰り返さないよう、BS15 を入れる前に現状の借金を減らす。**

| 借金 | 状態 | 出どころ |
| :-- | :-- | :-- |
| `coverage:effects` の実行実績0（継続効果11件） | 未着手 | HANDOFF §1 |
| `audit:semantics` S3（43件）・S4（57件）の未判定 | 未着手 | [SEMANTICS_AUDIT.md](./SEMANTICS_AUDIT.md) |
| 永久凍土の王都：自分でコストを払う4経路 | 未決（要ユーザー確認） | HANDOFF §2 |
| バトスピ Wiki との食い違い3件 | 未着手 | [RULES_BATSPI_WIKI.md](./RULES_BATSPI_WIKI.md) |

**S3・S4 は「前回無害と判定した分」と「新規」が混ざっていて区別できない**のが最大の障害。
まず判定済みを `S3_VERIFIED` / `S4_VERIFIED` に移して**新規だけが出る状態**を作る。
これをやらないと、以降どの弾でもこの2軸は使い物にならない。

---

## 1. データの取り込み

1. Wiki からカードリストを取得し `data/staging/BS15.json` に置く（BS14 と同じ形）
2. `npm run validate:cards` が通る構造にする（効果テキストは `effect` にそのまま入れる）
3. **cardId を機械検証する**（CLAUDE.md「重要な罠」。過去に ID が全面的にズレた事故がある）
4. 実装は色別 `data/staging/BS15-<色>.json` に分け、最後に結合して `data/cards/BS15.json` を作る
   （`scripts/merge-bs14.ts` を BS15 用に複製・改名する）

---

## 2. 解釈の確認（2026-09-16 作成。**ユーザー確認待ち**）

[CONJUNCTION.md](./CONJUNCTION.md) の早見表と、[SEMANTICS_AUDIT.md](./SEMANTICS_AUDIT.md) §3・
[COST_MODEL.md](./COST_MODEL.md) の既存規則を当てたうえで、**それでも読みが割れるもの**だけを並べる。
形式は「私の読み → 影響枚数」。**太字の番号が確認の要る論点**、それ以外は既存規則の適用で決まるもの。

### 2.1 弾全体に効く共通語彙（先に決めないと3バッチで3回発明される）

| # | 効果文 | 私の読み | 枚数 |
| :-- | :-- | :-- | --: |
| **1** | 「相手のフィールドのスピリット/ネクサスの**色1色につき**」「**色が2色以上**」 | 相手のスピリットとネクサスが持つ**色の種類数**（重複は数えない）。多色カードは各色を数える。**合体中のブレイヴの色も含む**。「◯色としても扱う」で得た色も含む。「色を無いものとして扱う」個体は数えない。**カードの色**で数え、シンボルの色ではない（SEMANTICS §3.16）。BS15-028 だけは「スピリットの色」なのでネクサスを数えない | 15 |
| **2** | 「自分のバーストをセットしている間、お互いのライフは、**ターンごとにスピリット1体から1までしか**減らされない」（神将6枚） | **1体のスピリットが1ターンに減らせるライフの合計が1**。同じスピリットが回復して2回目のアタックをしても0。**お互い**なので自分のアタックにも効く。アタックだけでなく**そのスピリットの効果によるライフ減少も合計に数える** | 6 |
| 3 | 「自分のフィールドに◯のスピリット/ネクサス**しかない**間」 | 自分のスピリットとネクサスが**すべて**その色を持つ（多色は含んでいればよい）。**1枚も無ければ成立しない**（「しかない」は存在を前提とする） | 7 |
| 4 | 虚神5枚「自分のバーストをセットしている間、手札にあるこのカードを**コスト7にする**」／X02「セットしていない間 **コスト11にする**」 | 2026-09-16 確定の規則どおり、**固定したコストからさらに軽減できる** | 6 |
| **5** | BS15-068「相手の手札にあるマジックカードすべてを**コスト+1する**」 | 「1コスト**余分に**支払う」（軽減の**あと**に足す）と違い、**カードのコストそのものを上げる**＝①総コストの段階で足し、**そこから軽減できる** | 1 |
| 6 | 「召喚する」（バーストで自身を召喚する札） | 「コストを支払わずに」が無ければ**コストを支払う**（SEMANTICS §3.7）。004 / 017 / 026 / 034 / 053 / 073 / X05 が該当 | 7 |
| **7** | 合体条件「コスト4以上**/**【暴風】を持つ」「コスト5以上/【粉砕】を持つ」 | 「/」は**または**（どちらか一方を満たせば合体できる） | 2 |

### 2.2 接続詞・解決順

| # | カード | 効果文（要旨） | 私の読み |
| :-- | :-- | :-- | :-- |
| **8** | 004 ハンゾウ／083 秘剣燕返／X05 カグヤ | 「A。**この効果発揮後**、B。」で **A の対象がいない／A の条件を満たさない** | **対象がいないだけなら A は発揮した扱い → B は起きる**（004：破壊する相手がいなくても召喚する／083：ブレイヴが無くてもコスト3以下を破壊する）。**条件文「〜とき」を満たさなければ A は発揮していない → B も起きない**（X05：トラッシュに黄3枚未満なら召喚しない） |
| **9** | 007 ゼニス・ドラグーン | 「BP5000以下を破壊する。手札1枚を破棄する**ことで、さらに**、BP10000以上を破壊する」 | 2つは**同時**なので、対象は先に両方決める。**前半の対象がいなくても後半は使える** |
| **10** | 022 アナグマッド・デビル | 「緑**しかないとき**、コア1個を置く。バースト1つを破棄する**ことで、さらに**、コア1個を置く」 | 「しかないとき」は**後半にも掛かる**（さらに＝前半への上乗せなので、前半が起きないなら後半も起きない） |
| **11** | 043 ショーグンペンタン | 「破棄したカードが黄のスピリットカードのとき残る。**さらに**、カード名に『ペンタン』を含むとき手札に加える」 | 2つの条件は**独立**。黄のスピリットでなくても、名前に「ペンタン」を含めば手札に加える |
| 12 | 008 パラディン・ドラゴン／055 マタドーラ | 「色1色を指定する。指定されなかった色のスピリットすべてを破壊する」 | 多色スピリットは**指定色を1つでも持てば残る**。008 は**お互いがそれぞれ自分のスピリットについて**指定し、**自分のスピリットも**破壊されうる。ターンプレイヤーから指定する |
| **13** | 035 軍神機メガ・テュール | 「相手は、指定した色のみになるように、相手のスピリット/ブレイヴを手札に戻す」 | 合体スピリットは**ホストとブレイヴを別々に**判定する（ブレイヴだけ色が違えばブレイヴだけ戻る） |
| **14** | 053 コジロンド・ゴレム／054 フェニックス・ゴレム／X06 サイゴード | 【粉砕】の破棄結果に反応する効果 | 053「コスト4以上のカードを破棄したとき」は**1回の【粉砕】につき1回**（何枚含まれても1体）。054「スピリットカード1枚**につき**」は枚数ぶん。X06「バースト効果を持つカードが破棄されたとき」は**1回だけ** |

### 2.3 新しい手順（どのタイミングで割り込むか）

| # | カード | 私の読み |
| :-- | :-- | :-- |
| **15** | X04 機獣要塞ナウマンガルド「自分のアタックステップ終了後、ドロー/リフレッシュ/メインステップのどれか1つを行う」 | アタックステップの後に**選んだステップを1回だけ追加で行い、そのあとエンドステップへ進む**（2回目のアタックステップは無い）。リフレッシュを選べば通常のリフレッシュと同じ処理（トラッシュのコアをリザーブへ・全回復） |
| **16** | 079 プロボケイション「相手のメインステップ終了時に使用できる」 | 相手がメインステップを終えるとき、**このカードを手札に持っていてコストを払えるときだけ**「使いますか」を聞く。持っていなければ何も聞かない（手札の有無が相手に漏れないよう、UI の待ち時間はつけない） |
| **17** | 045 虚獣帝スフィン・クロス「BPを比べるとき、アタックしているスピリットのコア1個をボイドに置くことで、BPを比べずにブロックされなかったものとして扱う」 | ブロックされたあと BP 比較の直前に聞く。使えば**どちらも破壊されず、ライフを減らす処理に進む**（「ライフを減らしたとき」の誘発も起きる） |
| **18** | 052 天蒼元帥チョウハッカイ「お互い、効果で手札を**増やす**/破棄することができない」 | 効果による**ドロー・手札に戻す・手札に加える**をすべて止める。**バウンス（スピリットを手札に戻す）も止まり、その場に残る**。「手札を破棄することで」のコストは払えない。ドローステップのドローは効果ではないので引ける |
| 19 | 072 渦巻く大海峡 | Lv1「リフレッシュステップ以外で回復できない」、Lv2「『破壊時』効果は発揮されない」は、主語が無いので**お互いのスピリット**すべてに効く |
| 20 | 004 ハンゾウ「破壊時、このスピリットをバーストとしてセットできる」 | 破壊待機中に解決し、**トラッシュに置かれる代わりに裏向きでバーストエリアへ**置く。コアは通常どおりリザーブへ |

### 2.4 確認を取らずに既存規則で決めたもの（異論があれば指摘してほしい）

- 076 妖華吸血爪「破棄した1枚につき、相手のスピリット1体のコア1個」は、**同じスピリットを何度選んでもよい**
- 075 ブラッディロンド：多い方の**持ち主が**、フィールド/リザーブ/トラッシュのどこからボイドへ置くかを選ぶ（ソウルコアも選べる）。スピリットのコアを0にすれば消滅する
- 026 軍師鳥ショカツリョー：疲労させる相手のスピリットは**使う側が選ぶ**（「相手は」が無い。CHOOSER_RULES）
- 018 霊獣皇テン・クー：「〜と同じ数**まで**」なので、召喚する枚数は0枚から選べる
- 044 天使サクエル：無償で使えるのは**1バトルにつき1枚**、フラッシュ効果だけ（バースト効果は使えない）
- 067 / 070「このターン相手が1回もアタックしてこなかったら」は BS04-050 と同じ判定

## 3. 新しい器が要るか

BS14 でバースト機構・`selfMode` / `subjectSide` 等は揃っている。
**まず既存の器で書けるかを確認する**。索引は次の1行で出る（型ファイルを丸読みしない）:

```
{ grep -o 'kind: "[a-zA-Z0-9_]*"' server/src/type.ts; grep -o 'type: "[a-zA-Z0-9_]*"' server/src/type.ts; } | sort -u
```

過去の実績では**1弾あたり約30種の新設アクション**が出ている（BS10:26 / BS11:26 / BS12:33 / BS13:33 / BS14:29）。
頭打ちになっていないので、BS15 でも同程度を見込む。**これが実装時間の実体**で、短縮は難しい。

---

## 4. バッチ分割（**2色で1エージェント**）

**⚠️ 色をまたぐ共通の器（§2.1 の #1〜#5 と #2 の神将サイクル）を、色バッチより先に作る。**
BS15 は「相手のフィールドの色の数」が15枚・「◯色しかない」が7枚・神将が6枚・虚神が6枚と、
**同じ語彙が全色に散っている**。色バッチに任せると3エージェントが別々に発明する
（`chooserIsTarget` が3枚で3回発明されたのと同じ形）。

| バッチ | 担当 | 中身 |
| :-- | :-- | :-- |
| **0** | 共通（メインループが設計を確定→1エージェント） | 色の数カウンタ／「しかない」条件／神将のライフ上限／虚神のコスト固定条件／コスト+N（軽減前） |
| 1 | 赤・紫（16+15） | 色指定破壊（008/055）、自身をバーストへ（004）、追加の【不死】まわり（064）、コア均等化（075） |
| 2 | 緑・白（15+15） | 追加ステップ（X04）、メインステップ終了時のマジック（079）、手札への色つきコスト増（068）、色指定バウンス（035） |
| 3 | 黄・青（15+15） | BP比較の省略（045）、手札の増減禁止（052）、回復制限と破壊時無効（072）、バースト条件の読み替え（069）、【粉砕】の破棄結果に反応（053/054/X06） |

### 新しく要りそうな器（着手時に grep で既存を確認してから決める）

既存で足りそうなもの: `colorlessThisBattle`（015/064）、`levelAs`（016/047/084）、`mustAttackByInstance`（032/079）、
`deckMillNegate`（028/030/042）、`funsaiBonus`（048/071）、`trashReturnAtEndStep`（077）、
`levelCostMod`（X02。015 はネクサス版が要る）、`magicFreeGrant`（044 の枠）、`hyoheki`（032）、`summonBurstCardFree`（バースト召喚）。

新設の見込み（約20種。過去5弾の平均26〜33種と同程度）: 色の数カウンタ、単色条件、神将のライフ上限、
コスト固定の条件、コスト+N（軽減前）、色指定→指定外を破壊/手札に戻す、自身をバーストとしてセット、
追加ステップ、メインステップ終了時の使用、BP比較の省略、バースト条件の読み替え、被デッキ破棄の1ターン上限、
回復制限、破壊時効果の全体無効、手札の増減禁止、トラッシュでの別名扱い、コア総数の均等化、
バースト発動の禁止（X03）、【粉砕】の破棄結果への反応、効果文の付与（054）

## 5. 検証（メインループが統合後に1回）

```
npm run typecheck && npm run validate:cards && npm run validate:notes && npm run validate:gaps && npm run smoke:quiet && npm run build:client
```

弾を入れ終わったら、定型に**入れていない**次も回す:

| コマンド | 見るもの |
| :-- | :-- |
| `npm run coverage:effects` | 書いたのに一度も発火していない効果 |
| `npm run audit:choices` | 対戦者が選ぶべき場面を実装が決めていないか |
| `npm run audit:semantics` | テキストと実装の解釈違い（**S6 は精度が高い**。S7 は疑い止まり） |
| `npm run audit:parity` | クライアントが出すボタンをサーバーが拒否しないか |

`npm run gaps:update` でベースラインを縮める。意図的に実装しない札は `data/card-notes.json` に理由を書く。

---

## 6. 出し方

**PR は1つの作業単位で出す**（2026-09-13 ユーザー指示）。BS15 は「弾の取り込み」という
同種の作業なので**弾ごと1つの PR でよい**。ただし次は分ける:

- 取り込みの過程で見つかった**既存カードのバグ修正**
- **監査ツール・smoke の仕組み**の改良
- **インフラ・ドキュメント**

`main` へマージすると **GitHub Actions が自動で Cloud Run へデプロイする**
（2026-09-13 に設定。[DEPLOY_CLOUDRUN.md](../ops/DEPLOY_CLOUDRUN.md)）。
手で `gcloud run deploy` を打つ必要はない。**マージ＝本番反映**である点に注意。

弾が入り終わったら `data/announcements.json` に1行足す（対戦者が読む文面。内部用語を書かない）。
