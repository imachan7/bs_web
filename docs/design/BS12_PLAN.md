# BS12「星座編 第三弾：月の咆哮」の取り込み計画

- 取り込み: 2026-09-03（`data/staging/BS12.json`。91枚）
- 進捗: **63 / 91枚 投入済み**（バッチ0＝器 A/B/D、バッチ1＝赤15枚、バッチ2＝紫14枚、バッチ3＝緑14枚、バッチ4＝白12枚）
- 内訳: スピリット55 / ブレイヴ12 / ネクサス12 / マジック12（多色2枚：BS12-040 黄白 / X008 赤白）
- 弾の `refer` は **「星座編 第三弾：月の咆哮」**（Wiki のカードリストから確定）
- 関連: [BS11_PLAN.md](./BS11_PLAN.md)／[BRAVE.md](./BRAVE.md)

---

## 0. この弾の性格

BS10・BS11 に続くブレイヴ弾の3作目。**シンボルの数**を数える・増やす・失わせるカードが集中し、
**【重装甲】**という新キーワードが入る。既存の器で書けるものが7割で、残りが新しい器。

新機構は次の4系統:

1. **【重装甲：色】**（7枚）— 【装甲】の上位。ブレイヴの効果も防ぐ
2. **シンボルの数**（8枚）— 「シンボル◯つを持つスピリット」の絞り込み／追加／喪失／数の比較
3. **【転召：星魂/ボイド】**（5枚）— 転召の「コスト指定」でなく「系統指定」版
4. **バニラ化・バニラ参照**（7枚）— 器は `vanillaAsGrant` があるので既存で書ける

---

## 1. 確定した解釈（2026-09-03 ユーザー確認）

| # | 論点 | 確定 |
| :-- | :-- | :-- |
| 1 | 【重装甲】は【装甲】の参照に含まれるか | **含まない。完全に別枠**。BS12-067 Lv2「【装甲】を持つ自分のスピリットすべて」は装甲持ちだけに効く（BS12-068 が「【装甲】/【重装甲】」と併記しているのが根拠）。`KEYWORD_INCLUDES` に足さないこと |
| 2 | BS12-X04【重装甲：可変】の「このスピリットの色」 | **付与色も含めて毎回算出**（`instColors`）。既存の【装甲：∞】`colorsFrom:"opponentFieldSymbols"` と同じ再計算の形で `colorsFrom:"selfColors"` を足す。BS12-027 Lv2 との組み合わせが効く |
| 3 | BS12-078 カシオペアシールのデッキ横のコア5個 | **コアは動かさずカウンタだけ持つ**（簡略化）。ボイドから出さない。`data/card-notes.json` に理由を書く |
| 4 | BS12-072 海賊王の秘宝島の主語 | **両陣営**（自分にも効く）。効果文に「相手は」が無いため。既存の主語なし `globalConstraint` と揃える |
| 5 | BS12-008「そのスピリットにアタックできる」（指定アタック） | **指定した1体が自動でブロッカーになる**。疲労状態でも指定でき、疲労のままブロック宣言する。【装甲】/【重装甲】持ちは指定できない。ブロック確定は**アタック時効果と【バースト】をすべて解決した後**で、それまでに指定先が場を離れる／条件を失うと**通常のアタックに戻る**（フラッシュタイミングは消えない） |
| 6 | BS12-X01 Lv3 の「赤のシンボル1つを追加する」 | **盤面のシンボル数に効く**（比較のときだけの仮想値にしない）。軽減・ライフダメージにも反映される |
| 7 | BS12-049「効果発揮中の『シール』を無効にする」 | **継続中の効果（endStepLock）を解除する**だけ。以後の使用は禁じない。トラッシュ・手札のカードには何もしない |
| 8 | BS12-061 Lv2「ブレイヴが召喚されたとき」の主語 | **両陣営**（「自分の」が無いため）。『自分のメインステップ』限定なので実質は自分のターン |

（2026-09-07 ユーザー確認。バッチ4・5ぶん）

| # | 論点 | 確定 |
| :-- | :-- | :-- |
| 9 | BS12-057 / 069「コアが0個になったとき、最高Lvとして破壊される」の「最高Lv」 | **そのカードのレベル表の最大Lv**（コア0＝Lv0を最大Lvの値に変換する）。**ただし『破壊時』効果は `levels` にその最大Lvを含むものだけが発揮する**。最大がLv3のカードで `levels:[2]` の『破壊時』しか持たないなら、その効果は**発揮しない** |
| 10 | BS12-037 / 058「Lv1/Lv2/Lv3BPを2000として扱う」の範囲 | **そのバトルの間、実効BPそのものを2000にする**（既存 `battleBpAsLevel` の「BP比較のときだけ」より広い）。対象条件（「BP◯以下」）にも反映される |
| 11 | BS12-070 Lv2「自分のライフは0にならない」 | **ライフ1で下げ止まる**（コアがそれ以上減らない）。減らなかったコアはライフに残る。条件は系統：**「天霊」**が5体以上 |
| 12 | BS12-081「効果すべてを失い、新たに得ることもない」＋「効果の記述を持たないスピリットとして扱う」 | **効果無効（`instEffectsSuppressed`）と バニラ化（`vanillaAsGrant`）の2本立て**。`vanillaAsGrant` は「バニラとして扱う」だけで発揮を止めないので（type.ts に明記）、片方では足りない |

（2026-09-07 ユーザー確認。バッチ6ぶん）

| # | 論点 | 確定 |
| :-- | :-- | :-- |
| 13 | **バニラ化（`vanillaAsGrant`）と自己言及の一般則** | 「効果の記述を持たないスピリットとして**も**扱う」なので、**バニラ化されても効果は発揮し続ける**（発生源自身も、他のスピリットも）。BS12-X06 Lv3 が自分自身を含んでも循環しない。バニラ**参照**（045 Lv2 / 071 Lv1 / 083 フラッシュ）の対象にはなる。#12 の BS12-081 が「効果を失う」のは別の一文（効果無効）が書いてあるからで、バニラ化の帰結ではない |
| 14 | BS12-043 Lv2「コスト3以下のスピリットすべての『このスピリットの召喚時』効果は発揮されない」の主語 | **両陣営**（「相手の」が無い。#4 と同じ線）。『相手のメインステップ』限定なので実質は相手だけ |
| 15 | BS12-072 Lv2「破棄できないとき、相手は、相手の手札すべてを公開する」 | **その場で見せて終わり**（一瞬の公開）。以後ずっと見えるわけではない |
| 16 | BS12-X06 召喚時「系統：「獣頭」を持つ自分のスピリットと同じコストの」 | **X06 自身も数える**（「このスピリット以外の」が無い。BS12-021 と同じ読み）。自身が獣頭・コスト7なので、コスト7の相手も対象になる |

（2026-09-07 ユーザー確認。バッチ7ぶん）

| # | 論点 | 確定 |
| :-- | :-- | :-- |
| 17 | X008「緑/白/黄のブレイヴとの合体時」「赤/紫/青のブレイヴとの合体時」の判定 | **合体しているブレイヴのいずれか1つが該当色を持てば成立**（多色ブレイヴは1色でも含めば該当＝`instHasColor` と同じ判定）。緑のブレイヴと赤のブレイヴを両方付ければ**2つの効果が両方使える** |
| 18 | BS12-040【合体時】「相手のスピリットが手札に戻ったとき」の主語 | **誰の効果でも発火する**（効果文に主語が無い）。相手自身の効果で相手のスピリットが戻った場合も含む。同カードの Lv1-3（マジック使用時に戻す）とは独立して働く |

---

## 2. 新しく要る器

| # | 器 | 対象カード | 形（案） |
| :-- | :-- | :-- | :-- |
| A ✅ | 【重装甲】 | 025 / 027 / 028 / 030 / 055 / X04 / 068 | `Keyword "heavyArmor"`。免疫判定で **sourceType:"brave" も防ぐ**（【装甲】は防がない＝type.ts §冒頭で設計済み）。`armorColorsGranted` と同じ経路を色ごとに持つ |
| B ✅ | 【転召：系統/ボイド】 | 007 / 015 / 024 / 040 / 047 | 既存 `keyword:"tensho"` エントリに `familyFilter` を足す（現状は `minCost`）。061 / 064 / 066 の「系統：星魂を持つコスト3のスピリット1体のコアすべてを置いたものとして扱う」＝既存 `tenshoCoreSubstitute` の系統版 |
| C ✅ | シンボルの追加（継続） | 006 | 新 kind `symbolAddGrant`（`colorAs`／`symbolFix` と同じ「場にありレベル有効の間」の継続。`steps:["ownAttack"]` で自分のアタックステップ限定） |
| C' | シンボルの喪失 | 080 | ターン中の一時。`CardInstance.tempSymbolLoss: Color[]` を `countSymbols` が引く |
| D ✅ | シンボル数フィルタ | 007 / 012 / 020 / 037 / 043 / 069 / X01 | `TargetFilter.symbolCount: number`（`instanceSymbolCount` で判定。BS11 の `detachOpponentBrave.minSymbols` と同じ数え方） |
| E ✅ | シンボル数の比較 | X01 | アタッカーとブロッカーのシンボル数の差だけ、相手のライフのコアをリザーブへ |
| F | リフレッシュのコア戻し上限 | 047 | `globalConstraint` に `reserveReturnCapForPid`（次の1回だけ。BS11 のリフレッシュ制限層に並べる） |
| G | 効果による召喚の禁止 | 072 | `globalConstraint` `noSummonByEffect`（両陣営。メインステップ限定） |
| H | マジックのコスト支払い元の制限 | 046 / 047 | `globalConstraint` `magicPayExclude: "spiritCores" \| "fieldCores"`（`paySources` の検証で弾く） |
| I | 5回のエンドステップ回復不可 | 078 | `CardInstance.noRefreshUntilOwnEndSteps: number`。デッキ横のコアは**ボイドから出さない簡略化**（§1 #3 で確認済み） |
| J | Lv1/Lv2/Lv3BP を 2000 として扱う | 037 / 058 | 既存 `battleBpAsLevel` の兄弟。バトル中だけ BP を定数へ上書き |
| K ✅ | 指定されなかったスピリットを全滅 | 015 | `mutualDestroyChoice` の否定版（お互い1体ずつ「残す」を選び、残り全部を破壊） |
| L | ライフが N になるようにコアを置く | 082 | `lifeCharge` の「N まで」版 |
| M | 手札が効果を受けない | 067 | `globalConstraint` `handImmuneForPid` |
| N | コア0で最高Lvとして破壊 | 057 / 069 | 破壊待機の Lv 決定への差し込み（[TIMING_CHART.md](./TIMING_CHART.md) §1.5） |
| O | 効果の記述を持つカードはターン1枚しか召喚できない | 071 | 既存 `summonLimitByCostForOpponent` の兄弟 |
| P | 合体スピリット破壊時、ブレイヴを残しスピリットだけ手札へ | 068 | 破壊処理への置換（`detachBravesOnLeave` の隣） |
| Q ✅ | 手札破棄で疲労状態でフィールドに戻る | 052 | 既存 `reviveOnDestroy` にコストと疲労状態を足す |
| R | 相手の手札と同じ枚数ドロー | 053 | `draw` の count をカウンタから取る |
| S | 「相手がドローしたとき」誘発 | 053 | `triggered` の新 event `opponentDrew` |
| T ✅ | 相手のリザーブに置かれるコアはトラッシュへ | X02 | コア移動先の置換（`globalConstraint`） |
| V | 相手によって破壊されたとき手札に戻る | X05 | 既存 `returnSelfToHand` を継続付与の形で |
| U | シンボル2つの合体スピリットのアタックではライフが減らない | 020 | 既存 `globalConstraint: noLifeDamageByCost` に `symbolCount` と `combinedOnly` を足す（両陣営） |
| W | 名前で絞ったコア除去耐性 | 022 | 既存 `coresCantBeRemoved`（両陣営・無条件）の「カード名で絞る・相手のスピリット/ブレイヴ/マジックの効果のみ」版 |
| X | デッキ1枚オープンして系統一致なら召喚、外れは破棄 | 065 | 既存 `revealTopSummonFreeOrHand` の兄弟（手札に加えるのでなく破棄する・系統で絞る）

---

## 3. 既存の器で書けるもの（新規実装なし）

001 003 004 005 008 009 010 011 013 014 016 017 018 019 021 022 023 026 029 031 032 033 034 035 036 038 039 041 042 044 045 048 049 050 051 054 056 058 059 060 061 062 063 064 065 066 070 073 074 075 076 077 079 081 083 084 X02 X03 X06 X008

---

## 4. バッチ割り（色ごと）

| バッチ | 対象 | 枚数 | 前提 |
| :-- | :-- | :-- | :-- |
| 0 | 器 A〜D（重装甲・転召系統・シンボル） | — | 他バッチの前に入れる |
| 1 | 赤 001-008 / 049-050 / 061-062 / 073-074 / X01 | 15 | バッチ0 |
| 2 | 紫 009-016 / 051-052 / 063-064 / 075-076 / X02 | 15 | バッチ0 |
| 3 | 緑 017-024 / 053-054 / 065-066 / 077-078 / X03 | 15 | バッチ0 |
| 4 | 白 025-032 / 055-056 / 067-068 / 079-080 / X04 | 15 | バッチ0（重装甲が集中） |
| 5 | 黄 033-039 / 057-058 / 069-070 / 081-082 / X05 | 14 | バッチ0 |
| 6 | 青 041-048 / 059-060 / 071-072 / 083-084 / X06 | 15 | バッチ0 |
| 7 | 多色 040 / X008 | 2 | 全バッチ |

---

## 5. 残っている節

バッチ1（赤）で確定した器: 指定アタックは**新設せず既存の `canDirectAttack` に `targetHighestBp` を足す**
（手順は TIMING_CHART.md §1.10）。シンボルの継続追加は `symbolAddGrant`（盤面のシンボル数に効く）。
継続中のマジックの解除は `negateContinuousMagicByName`（`endStepLock` を解除）。
【合体時】の起動能力は `kind:"activated"` + `whileCombined`（BRAVE.md §12.3）。

バッチ2（紫）で確定した器: 「指定されなかったスピリットを全滅」＝`mutualKeepChoice`（発生源自身は候補外）／
「相手のリザーブ行きのコアをトラッシュへ」＝`globalConstraint: coresToOpponentReserveGoToTrash`（両陣営の効果）／
『破壊時』の「疲労状態でフィールドに戻る」＝既存 `reviveOnDestroy` に `whileCombined` を足すだけ／
「色とシンボルは◯としても扱う」は **`colorAs` 1件で足りる**（`symbolFix` は置換なので使わない。SPEC.md §データを書くときの原則）。

バッチ3（緑）で確定した器: 「シンボル◯つの合体スピリットのアタックではライフが減らない」＝既存
`noLifeDamageByCost` に `symbolCount` ＋ `combinedOnly`／「カード名で絞ったコア除去耐性」＝
`coresCantBeRemovedByOpponent`（既存 `coresCantBeRemoved` は両陣営・無条件なので別枠）／
「デッキ1枚オープンして系統一致なら召喚、外れは破棄」＝`revealTopSummonFreeByFamily`／
「5回のエンドステップ回復不可」＝`CardInstance.noRefreshUntilOwnEndSteps`／
「ボイドからコアをリザーブか対象スピリット上へ」＝既存 `voidCoreToSelf` に `orReserve`（使用者が毎回選ぶ）。

**「破棄できる。そうしたとき〜」は1つのアクションに畳む**（BS12-053＝`discardHandAll.thenDrawOpponentHand`）。
2エントリに割ると、破棄しなかったときにドローだけが独立して走る余地が残る。
「そうしたとき」＝前後関係（CONJUNCTION.md）を1アクション内の文の並びで保証する。


バッチ4（白）で確定した器: 「相手のブレイヴの効果を受けない」＝`braveImmuneGrant`（【装甲】【重装甲】と**別枠の第3の軸**）／
「このスピリットが持つ【装甲】を与える」＝`armorEffectiveGrant`（`refreshLevelAsOverrides` の2パス目で、①で確定した実効の装甲色を配る）／
「“〜”という効果を与える」＝`effectEntryGrant`（誘発でなく常在エントリ本体を配る。`effectGrant` の兄弟）／
「合体スピリット破壊時にブレイヴを残しスピリットだけ手札へ」＝`reviveOnDestroy.combinedOnly`／
`globalConstraint: handImmuneForPid`／`CardInstance.tempSymbolLoss`／FieldEvent `ownHyohekiUsed`／
`forceAttackThisTurn` の `count:"any"` ＋ `requireOwnNameIncludes`／`braveHostUnblockableThisTurn`。

**「“【キーワード：色】〜という効果を与える”」はキーワード宣言も配る**（2026-09-07 ユーザー確認）。
効果文がキーワード表記ごと引用しているなら、振る舞い（`magicNegate` 等）だけでなく
`keywordGrant` も併せて書く。配らないと**そのキーワードを参照する他カードから見えなくなる**
（BS12-068 で実際に踏んだ。【氷壁】を参照するのは BS08-032 知将ゲンドリルと BS11-074 ソーラーブレイカー）。
`keywordGrant` には OR 版の `keywordFilterAny` がある（【装甲】/【重装甲】のように2つ以上を許すとき）。

## 6. バッチ5（黄）の下ごしらえ（設計のみ。実装は白バッチの後）

対象13枚: 034 / 035 / 036 / 037 / 038 / 039 / 057 / 058 / 069 / 070 / 081 / 082 / X05（033 は投入済み）。

**既存器で足りることが分かったもの**（§2 の当初見積もりから縮んだ）:

- **器L（ライフがNになるようにコアを置く。082）は既存 `lifeCharge` の `upTo`**（BS09-X35 超神星龍ジークヴルム・ノヴァ）
- 058 の「自分がマジックカードを使用したとき」＝既存 FieldEvent `ownMagicUsed`
- 081 のバニラ化＝既存 `vanillaAsGrant`（＋効果無効。§1 #12）
- 082 のトラッシュ耐性＝既存 `trashImmunity`
- 035 / 038 のライフへのコア配置＝既存 `lifeCharge`

**新しく要る器**: 器J（バトル中の実効BP＝2000。§1 #10）／器N（コア0で最高Lv破壊。§1 #9。057・069）／
器V（「相手によって破壊されたとき手札に戻る」の継続付与。X05 Lv2）／
「自分のライフは0にならない」（070 Lv2。§1 #11）／「◯体につき1体ずつ指定し、このターン ブロックできない」（038。
既存 `markCantBlockThisBattle` はバトル限定・単体なので兄弟が要る）／
バトルしている相手のシンボル数カウンタ（036）／コスト配列でのアタック禁止（X05 Lv2）。

**034 の「系統：「星魂」を与える」は既存 `familyGrant` で足りる**（2026-09-07 調査。ターン限定にする軸だけ確認する）。

## 7. バッチ6（青）の下ごしらえ（設計のみ。実装は黄バッチの後）

対象13枚: 042 / 043 / 045 / 046 / 047 / 048 / 059 / 060 / 071 / 072 / 083 / 084 / X06（041・044 は投入済み）。
この弾の「バニラ化・バニラ参照」がここに集中する。

**既存器で足りるもの**:

- 045 Lv1（系統「獣頭」）／046【合体時】／059【合体時】／X06 Lv3 ＝ すべて `vanillaAsGrant`（target と絞り込みだけ違う。§1 #13）
- 060 召喚時＝既存の `draw` 3枚 ＋ 手札破棄2枚（「その後」＝前後関係。CONJUNCTION.md）
- 048 召喚時＝`destroyAll` に コスト条件 ＋ 合体スピリット限定（`instIsCombined` 判定は既存）
- 042 Lv2「自分のネクサスすべては青のネクサスとしても扱う」＝`colorAs` のネクサス版
- 072 Lv1＝器G（`noSummonByEffect`。**両陣営**は §1 #4 で確定済み）
- 043 Lv1 の中身＝既存 `protectLifeByCostThisTurn`（片側限定版）に、緑バッチで `noLifeDamageByCost` へ足した `symbolCount` ＋ `combinedOnly` の片側版を足す

**新しく要る器**:

| # | 器 | 対象 |
| :-- | :-- | :-- |
| BA | `TargetFilter.vanilla?: true` — 効果の記述を持たないスピリットに絞る。**いまは `fieldEvent.vanillaOnly` しか無く `TargetFilter` に軸が無い** | 071 Lv1 / 083 フラッシュ / X06 Lv2 |
| BB | `levelAs` に「そのスピリットが持つ**最高Lv**として扱う」（`maxLevel: true`）。**「最高Lv」はレベル表の最大Lv**（§1 #9 と同じ読み） | 045 Lv2 |
| BC | 「相手の**マジック**の効果を受けない」耐性（色でなくカード種別で絞る）。白バッチの `braveImmuneGranted` と同じ第3の軸の隣に置く | 071 Lv1 |
| BD | 新 `globalConstraint`「コスト◯以下のスピリットすべての『召喚時』効果は発揮されない」。**両陣営**（§1 #14） | 043 Lv2 |
| F | `globalConstraint` `reserveReturnCapForPid` — **次の1回**の『相手のリフレッシュステップ』だけ、トラッシュ→リザーブの戻しを3個に制限 | 047 召喚時 |
| H | `globalConstraint` `magicPayExclude: "spiritCores" \| "fieldCores"` — マジックのコストの支払い元を制限（`paySources` の検証で弾く） | 046 Lv1 / 047【合体時】 |
| O | `summonLimitByCostForOpponent` の兄弟。コストでなく「**効果の記述を持つ**スピリットカード」でターン1枚に絞る | 071 Lv2 |
| BE | 042 Lv1「このターンの間、自分の**手札にある**ネクサスカードすべての軽減シンボルすべてを[青]として扱う」＝軽減シンボルの読み替え。手札のカードに効くので `handKeywordGrant` と同じ「場の発生源を判定時に見る」形にする | 042 Lv1 |
| BF | 「相手は手札のマジック1枚を破棄する（`chooserIsTarget`）。**破棄できないときのフォールバックで手札すべてを一瞬公開**」（§1 #15） | 072 Lv2 |
| BG | `destroy` の対象自動選択に「**最もコストが低い**」を足す（既存の既定は実効BP最大） | 084 |
| BH | 「系統◯を持つ自分のスピリットの**コストの集合**に一致する相手のスピリットすべてを破壊」。**発生源自身も数える**（§1 #16） | X06 召喚時 |
| BI | `onBattleEnd` ＋「効果の記述を持たない自分のスピリット1体を破壊することで回復する」（COST_MODEL.md：AとBの両方が完全に解決できるときだけ発揮） | X06 Lv2 |
| BJ | 『相手のアタックステップ』の**ステップ開始時**に、自身を疲労させて発動する起動（既存のステップ誘発 ＋ `cost:{exhaustSelf}`） | 043 Lv1 |
| 083 | `revealAndSummonKeyword` の兄弟＝デッキ上3枚公開し、**ネクサスカード**1枚をコストを支払わず配置、残りは破棄（後始末は既存 `revealDiscardRest` をそのまま使う） | 083 メイン |

## 8. バッチ7（多色）の下ごしらえ（設計のみ。実装は全バッチの後）

対象2枚: **BS12-040 天王神龍スレイ・カエルス**（黄白 / コスト6 / 神星・戯狩 / シンボル 黄白）と
**X008 神星皇ストライク・アポロドラゴン**（赤白 / コスト8 / 神星 / シンボル 赤白）。

**`X008` は誤りではない。** 弾の接頭辞を持たない X レア枠の既存の命名（X003D / X004 / X005R / X007）に揃っている。
`BS12-X008` に直さないこと。多色そのものの扱いは既に14枚の実績があるので新規実装は要らない。

**既存器で足りるもの**:

- 040 の【転召：星魂/ボイド】＝バッチ0の器B（`keyword:"tensho"` ＋ `familyFilter`）
- 040 Lv1-3「相手がマジックの効果を使用したとき、相手のスピリット1体を手札に戻す」＝
  既存 FieldEvent `opponentMagicUsed` ＋ `returnToHand`。**無効化されたマジックでも発火する**
  （`magicNegate` の型定義に「カード自体は通常どおり使用扱いで、『マジックの効果を使用したとき』の誘発は発揮される」と明記）
- X008 の1つ目＝BS12-X04 の e2 と同型（`fieldEvent` `anySpiritAttacked` ＋ `phase:"attack"` ＋ `turn:"opponent"` ＋ `selfMode:"source"` ＋ `refreshSelf`）
- X008 の2つ目の『このスピリットの**合体アタック時**』＝専用の器は無く、`triggered` `onAttack` ＋ `whileCombined:true` で書く（BS10-069 千刀鳥カクレインの前例）

**新しく要る器**:

| # | 器 | 対象 |
| :-- | :-- | :-- |
| BK | **合体しているブレイヴの色による発揮条件**（§1 #17）。`whileCombined` を持つエントリに `combinedBraveColors?: Color[]` を足し、`bravesOf` で合流させた各ブレイヴを `instHasColor` で見て**いずれか1つ**が該当すれば成立とする | X008 の2節 |
| BL | 新 FieldEvent `anySpiritReturnedToHand` ＋ 既存の `subjectSide:"opponent"` で絞る（既存は `ownSpiritReturnedToHand` しか無い。緑バッチの `anySpiritAttacked` ＋ `subjectSide` と同じ形に揃える）。「1体につき」は既存 `repeatPerCount` | 040【合体時】 |
