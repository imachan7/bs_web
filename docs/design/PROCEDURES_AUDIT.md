# 手順の棚卸し（「誰も決めていないのに決まっている」箇所の一覧）

2026-08-17 作成。**ゲームの手順が、どこまで文書化されていて、どこからは実装が勝手に決めているか**を
機械的に洗い出した記録。

> カード906枚の「効果の語彙」は SPEC.md に厚く書かれているが、**ゲームの「手順」はほとんど書かれていない**。
> そのため手順は `doSummon` の文の並び順のような形で暗黙に存在し、誰も決めていないのに決まっている状態になる。
> — CLAUDE.md

この文書はその「暗黙に決まっているもの」を可視化するためのもので、**正解を決める文書ではない**。
決まったことは各手順書（下の §2 の索引）へ移し、ここからは消す。

---

## 0. 結論（先に5行）

1. ~~文書にあるのに実装が従っていない規則が1つある：同時発揮の解決順~~
   → **2026-08-17 に解消**（→ §3）。誘発でもターンプレイヤーに解決順を聞くようにした
2. **対戦者が選ぶべき場面を実装が自動で決めている箇所が44件**ある（→ §4）
3. 手順書は9本あるが**索引がなかった**ので §2 に作った（同じ規則を何度も発見し直すのを防ぐ）
4. **既存の `audit:semantics` はこの層を見ていない**。データと効果文しか突き合わせないので、
   「ハンドラ実装が対象を勝手に選んでいる」は検出できない（→ §6 の S9 提案）
5. 同時発火（2件以上が同じタイミングで発揮）は smoke 全体で**38回だけ**＝稀。
   「順番を聞く」実装にしても対戦の煩雑さは小さい

---

## 1. 探し方（同じ棚卸しを再現するとき用）

```
# ① 実装が自覚している簡略化を全部出す（94件）
grep -rnE "簡略化|本来は|決め打ち" server/src/logic/**/*.ts

# ② そのコメントが属する ActionHandler の**範囲全体**に interactiveTargets の分岐があるかで二分する
#    分岐あり50件（テスト専用の自動化なので無害）／分岐なし44件（実対戦でも自動）
#
# ⚠️ 判定窓を「近傍40行」にすると誤検出が13件出る（2026-08-17 に実際に踏んだ）。
#    recoverSpiritFromTrash は選択式の分岐がコメントより40行以上上にあるため「自動」に見えた。
#    **ハンドラ範囲で見ること**（開始は ActionHandler<"…">、終わりは行頭の "}"）

# ③ 解決順を「ターンプレイヤーに聞いている」箇所を探す
grep -rn "destroyOrder|解決順" server/src/logic/*.ts   # → 破壊だけだった

# ④ 手順の語が文書のどこにあるか
grep -rl "<手順の語>" docs/design/*.md SPEC.md
```

**②の二分が要点。** 「決定的簡略化」は多くがテスト（`interactiveTargets:false`）を決定的にするための
ものなので、全部を問題視すると57件が94件に膨れて優先順位がつかなくなる。

---

## 2. 手順の索引（どの手順がどこに書いてあるか）

| 手順 | 書いてある場所 |
| :-- | :-- |
| アタックステップ（＞０〜＞７）・BP比較・バトル終了 | [TIMING_CHART.md](./TIMING_CHART.md) §1 |
| **ブロックされなかったアタックはバトルにならない**（『バトル終了時』が発火しない） | TIMING_CHART.md §1.9 |
| **シンボルの色とカードの色は別の値**（colors からシンボル色を導かない） | SEMANTICS_AUDIT.md §3.16 |
| **同時発揮の解決順（継続効果→誘発、同種はターンプレイヤーが決める）** | TIMING_CHART.md §0 ／ ⚠️ 実装は §3 のとおり未追随 |
| 破壊処理（破壊待機→誘発→トラッシュ→コア移動）・相打ちの体ごと解決 | TIMING_CHART.md §2・§3 |
| 効果文の接続詞（その後／さらに／このとき）の前後関係 | [CONJUNCTION.md](./CONJUNCTION.md) |
| 「〜することで」（コスト）の成立条件 | [COST_MODEL.md](./COST_MODEL.md) |
| 誰が選ぶか（「相手は」「自分は」の主語） | [CHOOSER_RULES.md](./CHOOSER_RULES.md) |
| 中断と再開・挿入順・召喚と【転召】の手順・**誘発ループの書き方** | [RESUME_STACK.md](./RESUME_STACK.md) §3・§6・§9 |
| どの層なら中断できるか（プレイヤーに選ばせたいとき） | [INTERRUPTION_POINTS.md](./INTERRUPTION_POINTS.md) |
| 「〜の効果で〜されたとき」の発生源の追跡 | [EFFECT_SOURCE_CONTEXT.md](./EFFECT_SOURCE_CONTEXT.md) |
| 誰が誘発効果を出しているか（A/B分類） | [TURN_EFFECT_SOURCES.md](./TURN_EFFECT_SOURCES.md) |
| 効果の解釈の点検（判定済みの一覧・確定した一般則） | [SEMANTICS_AUDIT.md](./SEMANTICS_AUDIT.md) |
| **『自分の◯◯ステップ』が自動発揮か任意発動か**（`step` と `activated` の使い分け） | SEMANTICS_AUDIT.md §3.6 |
| **効果による召喚でコストを支払うか**（「コストを支払わずに」の有無） | SEMANTICS_AUDIT.md §3.7 |
| **「自分の◯◯のスピリット」に自分自身が含まれるか** | SEMANTICS_AUDIT.md §3.8 |
| **フィールドのシンボルの数え方**（ネクサス込み・付与色の扱い） | SEMANTICS_AUDIT.md §3.9 |
| **継続効果がいつから有効になるか**（召喚時効果との前後） | SEMANTICS_AUDIT.md §3.10 |
| **「このターンの間、〜すべては」の範囲**（後から出たものにも効く） | SEMANTICS_AUDIT.md §3.12 |
| **「BPを+する効果は発揮されない」がどこまで止まるか** | SEMANTICS_AUDIT.md §3.13 |
| **「内容を見ないで選ぶ」はランダム**（決定的簡略化をしない） | SEMANTICS_AUDIT.md §3.14 |
| **ブレイヴの召喚は常に「スピリット状態／合体した状態」の選択方式**（手札からも効果からも） | [BRAVE.md](./BRAVE.md) §5.0 |
| **ネクサスの『配置時』は `trigger:"onSummon"`**（効果による配置で発揮するかは未決） | SEMANTICS_AUDIT.md §3.15 |
| **効果文が「選ぶ」なら実装も選ばせる**（自動選択を対戦の既定にしない） | この文書 §5 の一般則 |
| **任意コスト（「〜できる」）は発動確認を出す**（「〜する」は強制） | この文書 §5 Q3 ／ [COST_MODEL.md](./COST_MODEL.md) |
| ステップ進行・先攻1ターン目の例外・フラッシュ優先権・コスト支払い | SPEC.md §2「ルール」 |
| 未実装機構の先行設計（ブレイヴ／アルティメット／ソウルコア／バースト） | BRAVE / ULTIMATE / SOULCORE / BURST.md（**ブレイヴは 2026-08-24 に見直し済み**。置き場＝参照方式・分離のコアは選択式・エンジン先行を確定。未確定の論点は BRAVE.md §12） |

### この索引に**無い**もの（＝どこにも書かれていない決定）

- **誘発の走査順**：`fireStepTriggers` は「ターンプレイヤー側 → 相手側」、各プレイヤーの中は
  **`[スピリット…, ネクサス…]` の並び順（＝場に出た順）**。`fireFieldEventTriggers` と
  `fireBattleWonTriggers` は**片側しか走査しない**（呼び出し側が `pid` を決める）
- **ターン開始処理の区間の切り方**：`start → core → draw① → draw② → refresh → main` の6区間。
  ドローステップを①②に分けているのは「ドローより前に発火する効果」を成立させるため（`PhaseManager.ts` のコメント）
- **コア移動の粒度**：1個ずつか一括か（アクションごとにばらばら）
- **対象を決める時点**：効果の解決時に決めるのか、宣言時に決めるのか

---

## 3. 文書にあるのに実装が従っていなかったもの（**2026-08-17 解消**）

### 同時発揮の解決順（TIMING_CHART §0-3）

> A と B のそれぞれの中で複数が同時に発揮する場合は、**ターンプレイヤーが解決順を決める**

**この規則を守っているのは破壊処理だけ**（`PendingChoice.destroyOrder`。「フィールドに残る」と
【不死】が2体以上に出るならターンプレイヤーに順番を聞く）。

**誘発（`step` / `fieldEvent` / `battleWon` / `triggered`）は聞いていない。**
`resolveInOrder` が受け取った配列の順、つまり**場に出た順**で解決する。

| | 実装（修正前） | 実装（修正後） |
| :-- | :-- | :-- |
| 破壊の解決順 | ターンプレイヤーに聞く | 変更なし |
| 誘発の解決順 | 場に出た順で固定 | **ターンプレイヤーに聞く**（カード単位。同名2枚は対称なので聞かない） |

**決着した粒度は [TIMING_CHART.md](./TIMING_CHART.md) §0「実装状況」に1行として移した。**
回帰テストは `scripts/smoke/part217.ts`。

⚠️ **実装時に踏んだ落とし穴**：解決順を聞くために誘発の束を再開スタックへ積んだが、
**再開スタックは `act()` の解決ループでしか消化されない**。積んだだけでは `pendingChoice` が立たず、
呼び出し元から「何も起きなかった」ように見えて誘発が放置される。
`resolveInOrder` はその場で `resumeTriggerBatch` を呼んで**必ず `pendingChoice` を立てる**。

**実測**：smoke 全体で同時発火（2件以上）は**38回**（35回が2件、2回が4件、1回が6件）。
稀なので「聞く」実装にしても煩雑になりにくい。

---

## 4. ⚠️ 対戦者が選ぶべき場面を実装が自動で決めていないか

**一覧を手で持たない。`npm run audit:choices` が数え直す**（`scripts/audit-auto-choices.ts`）。
かつてここに44件の手書きの表があったが、直すたびに古くなり、
**残っていない項目を「次はこれ」と指していた**（2026-09-02 に数え直したら上位は対応済みだった）。

判定は「ハンドラ（と、そこから推移的に呼ぶ関数）に聞く経路が1つも無い」＋
「効果文に選択の語があるカードで使われている」。**誤検出を含む**ので1件ずつ読んで判断する:

| よくある誤検出 | 理由 |
| :-- | :-- |
| `ctx.resolve` で `destroy` などへ委譲しているもの | 委譲先が聞いているが、静的には追えない |
| マジックの `bpBuff` / `grantKeyword` など | **クライアントが対象を先取りする**（`renderer.ts` の `magicTargetSide`） |
| フラッシュ効果に「スピリット1体を」があるカード | 選択の語が別の節のもの。メイン側の効果には選択が無い |
| 「すべて」を対象にする効果 | そもそも選ばない |

**2026-09-02 に、実際に自動だった10か所を選択式にした**（part269 / part270）。
残りの検出はすべて上の誤検出パターンに当てはまることを確認済み。

⚠️ この検査は「実装者が自覚してコメントを書いた」ものに限らないが、
`ctx.resolve` 越しは追えないので**網羅ではない**。実対戦で「選べないのはおかしい」が出たら、
その action type を直接読むこと。

---

## 5. どこにも書かれていない決定（質問リスト）

「私はこう解釈しました → これでいいですか」の形で並べる。**答えが出たら §2 の索引先へ1行として移す。**

**Q1〜Q6 はすべて決着した（2026-09-02）。** 残っているのは実装だけ。

| # | 論点 | 決着 |
| :-- | :-- | :-- |
| ~~Q1~~ | ~~誘発が複数同時に発揮するときの解決順~~ | **2026-08-17 実装済み**。聞く。粒度は TIMING_CHART §0「実装状況」 |
| ~~Q2~~ | ~~対象選択の自動化~~ | **2026-09-02 ユーザー確定：全部選ばせる**（下の一般則） |
| ~~Q3~~ | ~~任意コストの自動発動~~ | **2026-09-02 ユーザー確定：効果文が「〜できる」なら発動確認を出す**。「〜する」は強制なので自動のまま。判定はカードデータの `optional` で行う |
| ~~Q4~~ | ~~「好きな順番で」の順序~~ | **2026-09-02 ユーザー確定：1つずつ選ばせる**（Q2 と同じ一般則） |
| ~~Q5~~ | ~~誘発の走査順~~ | **Q1 の決着で解消**。別カード同士なら聞くので、走査順は選択肢の並び順にしか影響しない |
| ~~Q6~~ | ~~レベル変動の反映は即時か~~ | **2026-09-02 決着：即時でよい**（`currentLevel` が都度計算する現状のまま。コア移動の途中で参照する効果は無い） |

### ⚠️ 一般則：効果文が「選ぶ」と書いているなら、実装も選ばせる（2026-09-02 ユーザー確定）

**候補が2体（2枚・2つ）以上あるなら、必ずプレイヤーに聞く。** 「実効BP最大」「コスト最大」
「フィールドの先頭」のような決定的な自動選択を、対戦の既定にしない。

- **非対話（`interactiveTargets === false`）では従来の自動選択をそのまま残す。**
  ここを変えると既存 smoke が大量に落ちる（INTERRUPTION_POINTS.md §5 の手順3）
- 候補が1つなら聞かない。0なら不発
- 書き方は `tryInteractiveTargetChoice` / `tryInteractiveCardChoice`（`EffectModules.ts`）に寄せる。
  「1つずつ選ばせて残りを再開スタックへ積む」形も同じヘルパーが持っている（`refreshOne` が手本）
- **順番を選ばせる場合も同じ**（Q4）。「好きな順番で」は1つずつ選ばせる

---

## 6. 次にやること

1. **§5 の質問に答えをもらい、§2 の索引先へ1行ずつ移す**
2. **`audit:semantics` に S9 軸（実装の自動選択）を足す**。既存の S1〜S8 は
   **データと効果文しか見ていない**ので、ハンドラ実装が対象を勝手に選んでいる層は死角になっている。
   検出案：効果文に「1体を選び」「好きな」「好きなだけ」等があるのに、対応するハンドラに
   `requestChoice` / `interactiveTargets` が無いものを候補にする
3. Q1 を実装するなら `PendingChoice` に「解決順の選択」を足す（`destroyOrder` と同じ形が既にある）

## 関連

- [SEMANTICS_AUDIT.md](./SEMANTICS_AUDIT.md)：効果の**解釈**の点検（この文書は**手順**の点検）
- [TIMING_CHART.md](./TIMING_CHART.md)：同時発揮の一般則の一次資料
- [RESUME_STACK.md](./RESUME_STACK.md) §9：誘発を複数解決するときの書き方
