# BS13「星座編 第四弾：星空の王者」の取り込み計画

- 取り込み: 2026-09-07（`data/staging/BS13.json`。**97枚**）
- 進捗: **17 / 97枚 投入済み**（赤18枚のうち17枚。残るは X005R 相当なし＝赤バッチ完了）
- 内訳: スピリット61 / ブレイヴ12 / ネクサス12 / マジック12。**多色は0枚**
- 色の配分: 赤18 / 紫16 / 緑16 / 白16 / 黄16 / 青16（※X005 系6色ぶんを含む）
- 弾の `refer` は **「星座編 第四弾：星空の王者」**
- 関連: [BS12_PLAN.md](./BS12_PLAN.md)／[BRAVE.md](./BRAVE.md)

---

## 0. この弾の性格

**新しいキーワードは1つも無い。** 出てくるのは【合体時】26 /【重装甲】7 /【不死】6 /【超覚醒】4 /
【呪撃】4 /【神速】4 /【転召】4 /【氷壁】2 /【強襲】2 /【合体中】2 /【激突】1 /【光芒】1 で、すべて実装済み。

代わりに**既存キーワードの引数が増える**のと、**ブレイヴの扱いがさらに一歩進む**のが主題:

1. **【不死】の条件指定**（2枚）— これまでの【不死】は無条件だったが、
   **【不死：コスト7/8】**（BS13-012）と**【不死：妖蛇】**（BS13-014）が出る。
   「何が破壊されたとき蘇るか」をコスト／系統で絞る
2. **スピリット状態のブレイヴへの干渉**（5枚以上）— アタック時に合体する（BS13-007）、
   ブレイヴ同士を合体させる（BS13-049）、相手のブレイヴだけを破壊する（BS13-X06）
3. **色を無いものとして扱う**（3枚）— BS13-011 / 015 / 052。色を**足す**のはあったが**消す**のは初出
4. **本来のコスト**（BS13-003）— 軽減前のコストで判定する軸

---

## 1. 確定した解釈

| # | 論点 | 確定 |
| :-- | :-- | :-- |
| 1 | BS13-007「合体できる。**その後**〜『合体アタック時』効果を発揮させる」 | 「その後」＝前後関係。**合体しなければ発揮しない**（2026-09-07 ユーザー確認） |
| 2 | 同上：**既に別のブレイヴが合体した状態**でアタックし、さらに1つ合体したとき | **新しく合体したブレイヴの効果だけ**発揮する（2026-09-08 ユーザー確認）。先に合体していたぶんはアタック宣言の時点で発揮済みなので、この効果は「宣言に間に合わなかった分を拾う」もの。二重発揮させない |
| 3 | BS13-049 が借りた効果の中の「このスピリット」 | **発揮する側（イリテバン）** を指す（2026-09-07 ユーザー確認） |
| 4 | BS13-005「分離できず、**お互い**、その上のコアは取り除けない」 | **「お互い」はコア除去にだけかかる**（2026-09-07 ユーザー確認）。分離禁止は主語なしで自然に両陣営 |
| 5 | X011「Lv1/Lv2/Lv3BP を12000として扱う」 | **場にいる間ずっと実効BPが12000**（2026-09-08 ユーザー確認）。BS12 の `setBattleBpFixed` はバトル中だけなので継続版が要る |
| 6 | 同上：置き換わるのは**印刷BPだけ**か、実効BP全体か | **印刷BPだけ**。効果文が「Lv1/Lv2/Lv3**BP**を12000として扱う」とレベルごとの印刷BPを名指ししているので、そのあとの BP+（ブレイヴの合体時BP+・オーラ・一時BP+）は12000の上に乗る。`battleBpFixed`（「BPを◯として扱う」＝実効BPごと固定）とはここが違う |
| 7 | BS13-061「BP4000以下のスピリットがアタックしたとき破壊する」 | **両陣営**（2026-09-08 ユーザー確認）。同じ弾の BS13-006 が「**相手の**スピリットが」と明記しているのと書き分けられている。自分のスピリットも破壊される |
| 8 | BS13-049 の「ブレイヴ同士の合体」で、できた塊はさらに合体できるか | **できない**（2026-09-08 ユーザー確認）。ブレイヴが乗った塊は、別のブレイヴを受けることも、本物のスピリットへ合体することもない＝**実体の入れ子は2段で止まる** |
| 14 | BS13-X03 Lv1-3「ブレイヴカードを召喚するとき、疲労状態で召喚する」でダイレクトブレイヴ（合体させて召喚）したとき | **合体先のスピリットが疲労する**（2026-09-08 ユーザー確認）。ダイレクトブレイヴは合体した状態で場に出るので実体は合体スピリット。合体時の疲労合成の規則（TIMING_CHART §1.3）とも整合する |
| 15 | BS13-X03 Lv3「**スピリットすべて**のコアは【転召】以外で取り除けない」の範囲 | **両陣営。自分のスピリットも対象で、自分の効果のコスト（コアをトラッシュへ置く等）も払えなくなる**（2026-09-08 ユーザー確認）。例外は【転召】だけ |
| 16 | BS13-053「このブレイヴを**回復させ、**そのスピリットに**合体できる**」 | **回復と合体はセット**（2026-09-08 ユーザー確認）。「できる」は効果全体にかかる任意で、発揮するなら両方行う。合体できない（合体条件を満たさない等）なら回復もしない |
| 17 | BS13-067「【重装甲】で指定された色の相手に破壊されたとき、疲労状態で自分のフィールドに戻る」 | **破壊は成立する。破壊待機状態へ移行し、破壊で誘発された効果をすべて処理してから、トラッシュに置かれる代わりに疲労状態でフィールドへ戻る**（2026-09-08 ユーザー確認）。「破壊されずに残る」（`reviveOnDestroy`）とは**発揮する誘発が違う**（[TIMING_CHART.md](./TIMING_CHART.md) §1.5 の破壊待機状態に乗る）。コア・レベルは待機解除と同じ扱い |
| 18 | BS13-030「このスピリットが持つ【重装甲】と同じ色の相手のスピリット1体ずつ」 | **色ごとに別の1体を選ぶ**（多色1体で両方の枠は埋めない）。**⚠️ 色を紫/青とハードコードしないこと**（2026-09-08 ユーザー確認）。【重装甲】はブレイヴなどで**後天的に付与される**ことがあるので、**そのスピリットがその時点で実際に持つ【重装甲】の色**を実行時に求める |
| 19 | BS13-067 Lv2「相手のメインステップを終了する」あとの進行 | **アタックステップへ進む**（2026-09-08 ユーザー確認）。ターンごと飛ぶのではなく、召喚・ネクサス配置ができなくなるだけ |
| 10 | 「このスピリットの色を無いものとして扱う」（011 Lv2 / 015 Lv2 / 052【合体時】） | **色とシンボルの両方が無色になる**（2026-09-08 ユーザー確認。2018年ルールマニュアル Ver.9.0 で「色を無いものとして扱う」→「色とシンボルは無色として扱う」に改定）。**そのスピリットのシンボルはどの色の軽減にも使えなくなる**。ライフ減少の枚数はシンボル数なので変わらない |
| 11 | BS13-011 Lv2『このスピリットのアタック時』の無色化はいつまで続くか | **そのバトルの終了まで**（2026-09-08 ユーザー確認）。`battleBpFixed` などと同じ寿命＝`clearBattle` でリセット |
| 12 | BS13-052 が借りた『破壊時』効果の「このスピリット」は誰か | **効果を持っているスピリット自身**（2026-09-08 ユーザー確認）。049（「このスピリットの効果として発揮できる」と明記あり）とは逆。**さらに、そのスピリットの現在Lvで有効なエントリだけ**が候補（Lv2の破壊時効果は、そのスピリットがLv2のときだけ発揮できる） |
| 13 | BS13-075「このターンの間、〜されたとき〜する」の回数 | **回数制限なし**（2026-09-08 ユーザー確認）。そのターン中、自分のスピリットが破壊されるたびに、コアを払えるかぎり毎回発揮できる |
| 9 | BS13-049 の借り元候補に**イリテバン自身**が入るか | **入る**（2026-09-08 ユーザー確認）。自身を選ぶと同じ効果がそのアタックで2回発揮される。ただし**2回で止める**（再帰的にまた自身を選べると無限に連鎖するため） |

## 2. 新しく要る器（調査中）

| # | 器 | 対象カード | 形（案） |
| :-- | :-- | :-- | :-- |
| A | 【不死】の条件指定 | 012（コスト7/8）／014（系統：妖蛇） | 既存 `keyword:"fushi"` に `costs?: number[]` と `familyFilter?` を足す。「何が破壊されたとき蘇るか」の絞り込み |
| B | 色を無いものとして扱う | 011 / 015 / 052 | `colorAs` の逆。`CardInstance` に色の打ち消しを持ち、`instColors` が引く。**【装甲】等の色参照が全部変わる**ので影響範囲が広い |
| C | 本来のコスト（軽減前）での判定 | 003 | `instMatchesCostFilter` の兄弟。軽減後でなくカード静的なコストを見る |
| D | アタック時に合体する | 007 | 『アタック時』に自分のスピリット状態のブレイヴと合体し、**その後『合体アタック時』効果を発揮させる** |
| E | ブレイヴ同士の合体 | 049 | 「このブレイヴがスピリット状態の間、このスピリットに自分のブレイヴ1つを合体させることができる」 |
| F | 相手のブレイヴだけを破壊 | X06 | 「相手のスピリット状態のブレイヴ1体か、相手の合体スピリットのブレイヴ1つ」 |
| G | 他のブレイヴの『合体アタック時』効果を借りて発揮 | 049 | 効果エントリの参照・実行 |
| H | 『破壊時』効果を破壊させずに発揮 | 052 | 破壊を伴わない誘発の実行 |
| I | トラッシュから手札に戻せない | 044 / 072 | `globalConstraint`。**相手だけ**（主語が「相手は」） |
| J | BP合計（系統）まで好きなだけ破壊 | 008 | 既存の「BP合計◯まで」の系統版カウンタ |
| K | ネクサスを Lv2 として扱う | 072 | 既存 `levelAs` のネクサス版（カード名で絞る） |
| L | 自分のトラッシュのこのカードがエンドステップに手札へ戻る | 015 | トラッシュのカード自身が持つ効果（`trashImmunity` と同じ層） |

※ この表は**調査の途中**。バッチ着手前に1枚ずつ読み直して確定させる。

## 3. 既存の器で書けるもの

新しいキーワードが無いぶん、**大半は既存の器で書けるはず**。§2 が確定してから列挙する。

## 4. バッチ割り（案）

BS12 と同じく**色ごと**。X005 系6色（A/D/E/S/T の5枚。**赤の X005R は BS10 に収録済みなので取り込まない**）は
同一カードの色違いで効果文が同じなので、**まとめて1バッチ**にすると効率がよい。

| バッチ | 対象 | 前提 |
| :-- | :-- | :-- |
| 0 | 器 A〜C（不死の条件指定・色の打ち消し・本来のコスト） | 他バッチの前 |
| 1〜6 | 色ごと | バッチ0 |
| 7 | X005 系5枚 ＋ X006 / X011 | 全バッチ |

## 5. ⚠️ 取り込みで踏んだこと

- **`X005R` は BS10 に既に収録されている**（Wiki が両方の弾に載せている）。staging から除外した。
  同じことが起きうるので、**取り込んだら必ず既存 cardId との衝突を検査する**
- **X005A/D/E/R/S/T は同一カードの6色違い**（「北斗七星龍ジーク・アポロドラゴン」）。
  デッキの上限は**カード名で合算**するので（`GameState.ts` の同名上限）、
  6色あっても**デッキには合計3枚まで**。実装側の追加対応は要らない
- 接頭辞を持たない X カード（X005系 / X006 / X011）は既存の命名どおり（X003D / X004 / X005R / X007 / X008）。
  `BS13-` を付けないこと

## 6. バッチ1a（赤・前半8枚）で足した器

`addSymbolPermanent`（本来のコストでの判定＝器C）／`combineOwnBrave`（`hostSelf` ＋
`thenFireWhileCombinedTrigger`＝器D。§1 #1・#2）／`destroyByBpBudget`（系統のBP合計まで＝器J）／
`awakenFromReserve` の `superAwakenOnly`（器N）／`cantSeparate`（器O）／
`returnOwnSpiritToHand`／`refreshSelfByReturnToHandFamily`。

**`refreshSelfByReturnToHandFamily` は独立アクションで正しい。** 2026-09-08 に一度
「`refreshSelf` のコスト軸へ畳むべきだった」と書いたが、**これは誤り**だった。
`refreshSelfByDestroyFamily`（「破壊することで回復する」）が BS08 の頃から独立アクションとして
存在しており（BS02-X07 巨神機トールが使用）、「〜することで回復する」を独立アクションで表すのは
**この codebase の確立されたパターン**。コスト軸へ畳むと同じ形に2通りの書き方ができて不整合になる。

⚠️ 一方 **`returnOwnSpiritToHand` は `returnToHand` に自分側限定の軸を足すほうが小さく済んだ**
（既存は `anySide` があるだけで自陣限定が無い）。差は小さいので作り直してはいないが、
次に自陣限定の手札戻しが来たら `returnToHand` を広げること。

**教訓: 「既存の器を広げるべきか、兄弟アクションを足すべきか」は、その形に前例があるかで決まる。**
前例（兄弟アクション）があるならそれに揃える。無いなら既存を広げる。


## 7. バッチ1b（赤・後半9枚）で足した器

対象: BS13-049 / 050 / 061 / 062 / 073 / 074 / X01 / X006 / X011。テストは `scripts/smoke/part299.ts`。

| 器 | 実装 | カード |
| :-- | :-- | :-- |
| E | `combineOwnBrave.hostSelf` を**スピリット状態のブレイヴ**がホストのときにも通す。塊は2段で止める（§1 #8） | 049 Lv1 |
| G | `borrowCombinedAttackEffect`（他のブレイヴの『合体アタック時』を1つ選んで自分の効果として発揮。自身も候補＝§1 #9） | 049【合体時】 |
| P | `constraint` に `combineLimit`。`shared/summon.ts` の `combineLimitFor` を RuleValidator と `braveCombineCandidates` の両方が読む | X01 Lv1-3 |
| Q | `kind:"bpAs"` ＋ `CardInstance.bpAsContinuous`（継続で印刷BPを置換。§1 #6） | X011【合体時】Lv2 |
| M | `TargetFilter.familyAll`（系統AND。`family` の配列＝OR とは別軸） | 061 Lv2 |
| R | `costMod` の `mode:"set"` を**手札のカード**へ（`familyFilter` ＋ `cardTypeFilter` で絞る） | 062 Lv1-2 |

そのほか小さい軸: `summonFromHandFree` の `combineNameIncludes` / `thenRefreshCombinedHost`（073）、
`revealAndSummonKeyword` の `familyFilter`（074）、`activated.cost.discardHandFamily`（062 Lv2）、
`bpBuff.thenAddSymbolThisBattle`（062 Lv2）、`countEffectCounter` の `ownLife` / `selfBraveCount`。

### ⚠️ ここで潰したバグ

- **スピリット状態のブレイヴが「自分自身と合体」できてしまった**。ブレイヴが合体先にもなれるように
  なった結果、候補の絞り込みを1か所忘れると `braveRefs` が自分を指す壊れた盤面ができる。
  `combineOwnBrave` の両経路で自身を外し、**`attachBrave` に `host === brave` の最終防波堤**を置いた
- **`bpAs` が BP+ を全部飲んでいた**（`effectiveBp` の早期 return）。§1 #6 のとおり印刷BPだけを置き換える形に直した

---

## 8. バッチ2（紫16枚）の設計 — 2026-09-08 確定

対象: BS13-009 / 010 / 011 / 012 / 013 / 014 / 015 / 016 / 051 / 052 / 063 / 064 / 075 / 076 / X02 / X005A。
確定した解釈は §1 #10〜#13。

### 8.1 新しく要る器

| 器 | 形 | 対象 |
| :-- | :-- | :-- |
| **S 無色化** | 「色とシンボルを無色として扱う」（§1 #10）。`instColors` が空配列を返し、`countSymbols`（軽減）がその個体を**まるごと飛ばす**。寿命は2つ：**バトル限定**（`CardInstance.colorlessThisBattle`。`clearBattle` で消す＝§1 #11）と**継続付与**（`kind:"colorlessGrant"`。`refreshLevelAsOverrides` が `colorlessContinuous` を毎回張り直す） | 011 Lv2（バトル）／052【合体時】（バトル）／015 Lv2（継続） |
| **T 不死の系統指定** | `keyword` の `fushi` に `triggerFamilies?: FamilyFilter` を足す。**既存の `triggerCosts` と同じ枠の兄弟軸**（コスト指定は既にある＝012 はデータだけで書ける） | 014【不死：妖蛇】 |
| **U 不死経由限定の召喚時** | `triggered` の condition に「その召喚が【不死】によるものだったときだけ」。**`fieldEvent` 側には既に `fushiSummonOnly` がある**ので、同じ判定を `triggered` の `trigger:"onSummon"` でも読めるようにする | 014 召喚時 |
| **V 破壊時効果の借用** | 自分のスピリット1体を選び、**そのスピリットが持つ『破壊時』効果1つを、破壊させずにそのスピリット自身が発揮する**（§1 #12）。器G（049 の合体アタック時借用）と違い、**発揮する主体は借り元**。候補は**借り元の現在Lvで有効なエントリだけ** | 052『このブレイヴの召喚時』 |
| **W 相手による破壊限定** | `triggered` の `trigger:"onDestroy"` に「相手の効果／相手のスピリットによる破壊のときだけ」の条件軸。`destroyContext` に破壊した側の情報があるのでそれを見る | 010 Lv2 |
| **X コア数での絞り込み** | `TargetFilter` に `maxCores`（そのスピリットの上のコア数がこれ以下）。`fieldEvent` の `anySpiritAttacked` から使う。**両陣営**（061 で確定した一般則をそのまま適用＝「相手の」と書いていない） | 063 Lv1-2 |
| **Y ドロー枚数の追加** | ドローステップ開始時に、**コストを払ってその回のドロー枚数を+2する**。既存の `draw` とは別で、**通常ドローの枚数そのもの**を増やす | 063 Lv2 |
| **Z ライフ減少先の置換** | 「相手のライフのコアはリザーブではなくボイドに置かれる」。継続の `constraint` として持ち、ライフを減らす処理が置き先を決めるときに読む。**条件つき**（系統「妖蛇」を持つ Lv2 以上の自分のスピリットのアタックによる減少のときだけ） | 064 Lv1-2 |
| **AA 回復状態で残る** | `reviveOnDestroy` に2軸: `familyFilter`（`scope:"ownAll"` 用。既存の colorFilter / keywordFilter の兄弟）と、**バトルによる破壊限定**＋**残ったスピリットを回復状態にする** | X02 Lv2-3 |

### 8.2 既存の器で書けるもの

| カード | 節 | 使う器 |
| :-- | :-- | :-- |
| 009 | 効果なし（バニラ） | `effects: []` |
| 010 | トラッシュのコスト1以下2枚まで無償召喚できる | `summonFromTrashFree`（`count:2` / `costFilter.max:1` / 任意） |
| 012 | 【不死：コスト7/8】 | `keyword` の `triggerCosts:[7,8]`（**既存**） |
| 012 | 破壊時：相手のスピリットのコア1個を相手のトラッシュへ | `coreRemove`（`dest:"trash"`） |
| 013 | 召喚時：相手のコスト3以下**すべて**の上のコア1個ずつをリザーブへ | `coreRemove` の全体版（`filter.costFilter.max:3` ＋ 対象すべて） |
| 014 | 不死経由の召喚時：トラッシュのコスト7以上1枚を無償召喚（召喚時効果は発揮しない） | `summonFromTrashFree`（`costFilter.min:7` / `skipOnSummon` / 任意） |
| 015 | トラッシュのこのカードは『自分のエンドステップ』に手札に戻る | **要調査**。既存に無ければ小さい `step` エントリを1つ足す |
| 015 | 召喚時：トラッシュの【呪撃】/【不死】1枚を手札へ | トラッシュ回収系に `keywordFilter`（配列＝OR） |
| 016 | 妖蛇が召喚されたとき1ドロー | `fieldEvent` `ownSpiritSummoned` ＋ `familyFilter` ＋ `draw` |
| 016 | 竜人が疲労したとき相手のコア1個をリザーブへ | `fieldEvent` `ownSpiritExhausted` ＋ `familyFilter` ＋ `coreRemove` |
| 016 | 破壊時：トラッシュの「ドラゴナーガ」すべてを手札へ | トラッシュ回収系の `nameContains` ＋ 全部 |
| 051 | 自分のスピリット1体を破壊することで、**相手は**相手のスピリット1体を破壊する | 破壊コスト ＋ `destroy` の `chooserIsTarget`（[CHOOSER_RULES.md](./CHOOSER_RULES.md)） |
| 064 | 相手のアタックで自分のライフが減ったとき、アタッカーのシンボル1つにつき相手が手札1枚を破棄 | `fieldEvent` `ownLifeDamaged` ＋ 手札破棄の `countCounter`（シンボル数） |
| 075 | メイン：このターンの間、自分のスピリットが破壊されたとき、コア1個を払って妖蛇1枚を無償召喚（**回数制限なし**＝§1 #13） | `lendSelfThisTurn` ＋ `fieldEvent` `ownSpiritDestroyed`（`lentOnly`）＋ `summonFromTrashFree`（`skipOnSummon`） |
| 075 | フラッシュ：このターンの間スピリット1体をBP+2000 | `bpBuff`（`anySide`） |
| 076 | 疲労状態の相手の合体スピリット1体 **または** 疲労状態の相手のコスト3以下2体を破壊 | 2択の選択肢 ＋ `destroy`（`filter.combined` / `filter.costFilter.max:3`） |
| X02 | 召喚時：星魂1体を破壊することで3ドロー | 破壊コスト ＋ `draw` |
| X02 | Lv3 ブロック時：ブロックした相手のコアすべてをボイドへ | `coreRemove`（`dest:"void"` / 全部）。**[ソウルコア]は未実装なので「以外」は考慮しない**（既存の `discardOpponentTegamotoVoidCoresPer` と同じ扱い） |
| X005A | 召喚時：手札のブレイヴ1枚を無償召喚でき、召喚されたら1ドロー | `summonFromHandFree`（`bravesOnly` ＋ `thenDraw:1`。**両方とも既存**） |
| X005A | 【合体時】Lv3 アタック時：BPを比べ相手だけを破壊したとき相手1体を破壊 | `battleWon`（`role:"attacker"` / `whileCombined`）＋ `destroy` |

---

## 9. バッチ3（緑16枚）の設計 — 2026-09-08 確定

対象: BS13-017 / 018 / 019 / 020 / 021 / 022 / 023 / 024 / 053 / 054 / 065 / 066 / 077 / 078 / X03 / X005E。
確定した解釈は §1 #14〜#16。

**⚠️ 紫バッチ（§8）の後に着手すること。** 器を2つ共有する: **W 相手による破壊限定**（066 Lv1-2 が
紫 010 と同じ形）と、**トラッシュの自己回収**（077 が紫 015 と同じ形。077 は「ターンに1回」の制限つき）。

### 9.1 新しく要る器

| 器 | 形 | 対象 |
| :-- | :-- | :-- |
| **AB 疲労状態での召喚強制** | 「お互い、条件を満たすカードを召喚するとき、疲労状態で召喚する」。両陣営に効く `globalConstraint`。**ダイレクトブレイヴでは合体先のスピリットが疲労する**（§1 #14） | 065 Lv1-2（系統「遊精」/「星魂」を持たないコスト3以下のスピリットカード）／X03 Lv1-3（系統「遊精」を持たないスピリットカード**とブレイヴカード**） |
| **AC コア除去禁止の例外軸** | 既存 `coresCantBeRemoved`（BS10-X01。自身のコアだけ）を広げる。**対象範囲**（相手すべて／両陣営すべて）と**例外**（「相手の効果以外」＝持ち主の効果だけ許す／「【転召】以外」）の2軸 | 065 Lv2（相手のスピリットすべて・相手の効果以外）／X03 Lv3（**両陣営すべて・【転召】以外。自分のコストも払えない**＝§1 #15） |
| **AD 「回復したとき」の誘発** | `FieldEvent` に「自分のスピリットが回復したとき」を足す（既存の `ownSpiritExhausted` の対になる）。発生源自身を除く軸つき | 024 Lv2（「[武神獣ディアル・ユキムラ]以外の自分のスピリットが回復したとき」） |
| **AE 手札に戻すことがコスト** | 「〜を手札に戻すことで〜する」（[COST_MODEL.md](./COST_MODEL.md) §1：両方完全に解決できるときだけ発揮）。**3枚で使うので1つの軸にまとめる** | 019 Lv1-2（【神速】持ち1体を戻して自身を回復）／023【合体時】Lv3（このスピリットのブレイヴ1つを戻して自身を回復）／053【合体時】（【神速】持ち1体を戻して相手1体を手札に戻す） |
| **AF 疲労させたスピリットのBPぶんBP+** | 「自分のスピリット1体を疲労させることで、このスピリットをBP+(疲労させたスピリットのBP)する」。既存 `bpBuff.amountFromSelfBp`（**発生源自身**のBP）の**コストで選んだ対象**版 | 024『このスピリットのアタック時』 |
| **AG 【神速】召喚に反応して回復＋合体** | 【神速】を持つ自分のスピリットが召喚されたとき、**バトルしていない**スピリット状態のこのブレイヴを回復させ、そのスピリットに合体する（**セット**＝§1 #16） | 053 Lv1 |

### 9.2 既存の器で書けるもの

| カード | 節 | 使う器 |
| :-- | :-- | :-- |
| 017 | アタック時：遊精1体につき自身をBP+1000 | `selfBuffPer`（系統カウンタ） |
| 018 | 効果なし（バニラ） | `effects: []` |
| 019 | Lv2：自分のスピリットが合体したとき、そのスピリットを回復させる | `fieldEvent` `anySpiritCombined`（**self に合体スピリットが渡る**）＋ 回復 |
| 020 | アタック時：自身の上のコア1個につきBP+1000 | `selfBuffPer`（自身のコア数カウンタ） |
| 021 | フラッシュ【神速】／破壊時：自分のスピリット1体を回復 | `keyword` の `soku`（**既存**）＋ `triggered` `onDestroy` ＋ 回復 |
| 022 | 破壊時：ボイドから**このスピリットのLvと同じ個数**のコアをリザーブへ | `voidCoreToSelf` の `orReserve` ＋ レベルのカウンタ |
| 023 | Lv1-3 エンドステップ：Lv2以上の自分のスピリットすべてを回復 | 全体回復（`filter` にレベル下限） |
| 024 | 召喚時：遊精1体につきボイドからコア1個をリザーブへ | `voidCoreToSelf` の `orReserve` ＋ 系統カウンタ |
| 053 | 【合体時】バトル時：BPを比べ相手だけ破壊したとき〜相手1体を手札に戻す | `battleWon`（`role:"attacker"` / `whileCombined`）＋ `returnToHand` ＋ 器AE |
| 054 | 『このブレイヴの召喚時』**相手は**、相手のスピリット2体を疲労させる | 疲労の `chooserIsTarget`（[CHOOSER_RULES.md](./CHOOSER_RULES.md)） |
| 066 | Lv1-2：**相手によって**自分のスピリットが破壊されたとき、ボイドからコア1個をリザーブへ | **紫の器W** ＋ `voidCoreToSelf` の `orReserve` |
| 066 | Lv2：相手のアタックで自分のライフが減ったとき、コア1個をリザーブへ置き、自分のスピリット1体を回復 | `fieldEvent` `ownLifeDamaged` ＋ 上と同じ ＋ 回復（「置き、〜する」＝同時） |
| 077 | トラッシュの[ブリーズライド]1枚は『自分のエンドステップ』に手札に戻る（ターンに1回） | **紫 015 の器**（自己回収）＋ `oncePerTurn` |
| 077 | フラッシュ：このターンの間スピリット1体をBP+3000 | `bpBuff`（`anySide`） |
| 078 | このターンの間、自分のスピリット1体に“自分のスピリットがBPを比べ相手だけを破壊したとき、このスピリットは回復する”を与える | `targetChoiceLendThisTurn`（**既存**）＋ `battleWon` を `target:"chosenInstance"` で貸す |
| X03 | Lv2-3『アタック/ブロック時』BPを比べ相手だけ破壊したとき、光導/星魂1体を回復 | `battleWon` ＋ 回復（`familyFilter` 配列＝OR） |
| X005E | 北斗七星龍ジーク・アポロドラゴン（緑） | **X005A（紫）と効果文が完全に同じ**。紫バッチで書いたエントリをそのまま写す |

---

## 10. バッチ4（白16枚）の設計 — 2026-09-08 確定

対象: BS13-025 / 026 / 027 / 028 / 029 / 030 / 031 / 032 / 055 / 056 / 067 / 068 / 079 / 080 / X04 / X005D。
確定した解釈は §1 #17〜#19。主題は**【重装甲】**・**疲労状態でのブロック**・**ライフを守る**・**手札／デッキへ戻す**。

### 10.1 新しく要る器

| 器 | 形 | 対象 |
| :-- | :-- | :-- |
| **AI 破壊待機からフィールドへ戻す** | 破壊待機状態のスピリットを、トラッシュに置かれる代わりに**疲労状態でフィールドへ戻す**（§1 #17）。条件は「**BPを比べ**（＝バトルによる破壊）」かつ「破壊した相手のスピリットの色が、そのスピリットの**【重装甲】指定色**に含まれる」 | 067 Lv1-2 |
| **AJ 【重装甲】の指定色を実行時に読む** | 「このスピリットが持つ【重装甲】と同じ色」を、**静的なカード記述ではなくその時点の付与込みで**求めるヘルパー（既存の `heavyArmorColorsGranted` を読む）。**色をハードコードしない**（§1 #18）。求めた色ごとに1体ずつ処理する | 030【合体時】Lv2（色ごとに1体ずつ手札へ）／067（AI の判定にも使う） |
| **AK メインステップの強制終了** | 相手が効果でドローしたとき、**その効果発揮後**に相手のメインステップを終了して**アタックステップへ進める**（§1 #19）。「この効果発揮後」＝誘発と派生をすべて解決してから（[CONJUNCTION.md](./CONJUNCTION.md)） | 067 Lv2 |
| **AL 疲労状態でブロックできる** | 「自分の〈条件〉のスピリットすべては、〈条件〉の相手のスピリットを**疲労状態でブロックできる**」。ブロック可否の判定（`shared/block.ts`）に、疲労を無視できる継続付与を足す。**ブロックできる相手側の条件**（BP上限／合体スピリット／すべて）も持つ | 029 Lv1-3（自分の赤すべて・BP6000以下）／029 Lv2-3（同・相手の合体スピリット）／031 Lv1-2（自分の甲竜すべて・相手すべて） |
| **AM デッキ破棄の上限** | 「お互いのデッキは、効果では**ターンに3枚までしか破棄されない**」。既存の `millCapBonusFor`（上限を上げる）の逆で、**上限を設ける**軸 | 026 Lv1-3 |
| **AN ライフを守る2種** | ①「このターンの間、**ブロックされなかった指定したスピリットのアタック**では自分のライフは減らされない」（対象を1体指定して1ターン）／②「**相手のスピリットの効果では**自分のライフは減らされない」（継続。既存の `lifeImmuneThisTurn` とは発生源の種類で絞る点が違う） | 027 Lv1-2（①。自身を手札に戻すことがコスト）／027 Lv2（②） |
| **AO 手札に戻る先をデッキの上へ置換** | 「このターンの間、自分の効果で手札に戻るスピリットすべては**持ち主のデッキの上に戻る**」。手札に戻す処理の行き先を1ターンだけ差し替える | 079 メイン |
| **AP キーワードの継続付与** | 「このブレイヴがスピリット状態の間、自分のスピリットすべてに“【重装甲：赤】…”を与える」。**要調査**：既存のキーワード付与で書けるなら足さない | 056 Lv1 |
| **AQ アタック回数の制限** | 「お互い、**シンボル2つを持つスピリット**はターンに1回しかアタックできない」。両陣営の `globalConstraint` | 068 Lv1-2 |

### 10.2 既存の器で書けるもの

| カード | 節 | 使う器 |
| :-- | :-- | :-- |
| 025 | 効果なし（バニラ） | `effects: []` |
| 026 | フラッシュ：自身を疲労させることで、このターン光導/星魂すべてをBP+3000 | 疲労コスト ＋ 全体BP+（`familyFilter` 配列＝OR） |
| 028 | 【氷壁：赤/緑/黄】 | `keyword` の `hyoheki`（**既存**） |
| 028 | Lv2-3 相手のアタックステップ開始時、【氷壁】持ちすべてを回復 | `step` ＋ 全体回復（`keywordFilter`） |
| 029 | Lv3 このスピリットは赤のスピリットとしても扱う | `colorAs`（**既存**） |
| 030 | Lv1-2【重装甲：紫/青】 | `keyword` の重装甲（**既存**） |
| 031 | Lv2 ブロック時：甲竜1体につき自身をBP+2000 | `selfBuffPer`（系統カウンタ） |
| 032 | 【転召：武装/ボイド】 | `keyword` の `tensho`（`familyFilter` ＋ `dest:"void"`。**既存**） |
| 032 | 召喚時：相手のネクサスすべてを手札に戻す | `returnNexusToHand`（`all` ＋ `side:"opponent"`） |
| 032 | 【合体時】Lv3：BP6000以上の相手からブロックされない | ブロック不可の継続（BP下限つき） |
| 055 | 『このブレイヴの召喚時』相手のネクサス1つをデッキの上に戻す | ネクサスをデッキ上へ（**要調査**。手札版は既存） |
| 055 | 【合体時】【重装甲：緑/白/黄】 | `keyword` の重装甲 ＋ `whileCombined` |
| 056 | 【合体時】ブロック時：バトルしている相手のシンボル1つにつき自身をBP+5000 | `selfBuffPer`（相手のシンボル数カウンタ） |
| 068 | Lv2：BP4000以下の相手がアタックしたとき、このネクサスを疲労させることで手札に戻す | `fieldEvent` `anySpiritAttacked`（`maxBp:4000`）＋ 疲労コスト ＋ `returnToHand` |
| 079 | フラッシュ：このターンの間スピリット1体をBP+2000 | `bpBuff`（`anySide`） |
| 080 | 相手1体を手札に戻す。**その後**、カード名に「ストライク」を含む自分のスピリットがいるとき、もう1体戻す | `returnToHand` ×2（2つめに名前カウンタの条件。「その後」＝前後関係） |
| X04 | Lv1-3【重装甲：紫/緑/白/黄】 | `keyword` の重装甲（**既存**） |
| X04 | Lv1-3：自身以外の光導/星魂が疲労したとき、このスピリットは回復する | `fieldEvent` `ownSpiritExhausted`（`familyFilter` ＋ 自身を除く）＋ 回復 |
| X04 | 【合体中】Lv3：光導/星魂すべてに白のシンボル1つを追加 | `symbolAddGrant`（**既存**。BS13-062 で使った枠） |
| X005D | 北斗七星龍ジーク・アポロドラゴン（白） | **X005A（紫）と効果文が完全に同じ**。紫バッチで書いたエントリをそのまま写す |
